# ALG-007 — Multi-axis Recovery / 多轴恢复

前一版 Task Spec 设计已通过 PR #17 review 并集成，v0.5.0-019 已 **MERGED / INTEGRATED**。
本版增加 **v0.5.0-020A relative roll-rate prerequisite design freeze**：共享字段的兼容迁移、
派生不可用语义、符号 wiring、同采样与生命周期规则已冻结；精确 near-singularity 数值界限因
缺少独立误差/工作包络证据而保持 **NOT FROZEN**。软件向量规则及合成动态 fixture 沿用
v0.5.0-019；共享 roll-rate 仍 **PLANNED / NOT IMPLEMENTED**，非零可执行命令投影仍是独立
前置依赖。ALG-007 Candidate **BLOCKED / NOT IMPLEMENTED**。本文不是 Candidate、完整冻结的
Formal Qualification Profile、Candidate Result、真实执行器分配或硬件授权。场景与设计状态见
[Algorithm Benchmark / ALG-007 planned profile](../ALGORITHM_BENCHMARK.md#14-alg-007-planned-profile-design-v1)。

## 1. Status、baseline 与目标

| 字段 | 值 |
| --- | --- |
| Task / Milestone | `ALG-007 / M3 — Full Balance Recovery` |
| Task Spec | `v1 design INTEGRATED；v0.5.0-019 MERGED / INTEGRATED；v0.5.0-020A ROLL-RATE PREREQUISITE DESIGN FROZEN EXCEPT NUMERICAL GUARD` |
| Previous design baseline / integration | `607eefb85c31e8da35b884a6059dc00d62deba18` / `f4b08f16fa1eee1510046b3e2c09b7587f5b6131` |
| Current work-unit baseline | `origin/develop@ae27a2b0e3dd17ccfb111fdc2c61842ba9cc53bf` |
| Inherited status | `ALG-001～006 QUALIFIED / INTEGRATED` |
| Benchmark | `Algorithm Benchmark v1 / ALG-007 pre-Candidate contracts；full Profile NOT FROZEN` |
| Candidate / Candidate Result / Formal Qualification | `BLOCKED, NOT IMPLEMENTED / NOT CREATED / NOT EXECUTED` |
| Hardware access / authorization / validation | `NO / NONE / NOT AUTHORIZED, NOT EXECUTED` |

唯一新增软件能力是对同一采样时刻的 **pitch 与 roll 联合恢复需求** 作确定、有限、受限且
可诊断的处理，包括同时扰动、合成耦合压力、共享预算竞争和未满足需求。ALG-008 保留完整
扰动恢复与更广泛动态场景；本任务不改变真实硬件控制行为。

## 2. 已审计的软件事实和非目标

| 问题 | 当前证据与设计结论 |
| --- | --- |
| 输入/输出能否表示双轴 | `FlightState.imu` 有 `relative_pitch_rad`、`relative_roll_rad`；只有统一的 `relative_pitch_rate_rad_s`，没有公开的相对 roll rate。`FlightCommand` 是完整 absolute motor rad 目标与左右 `[0,1]` fan 命令，没有抽象双轴 intent 字段。 |
| Controller 是什么接口 | `FlightController.update(state, dt)` 不规定轴数；ALG-001～006 当前实现和 task-local seam 均只处理 pitch，因此实现是单轴，接口本身隐式允许多轴读取。工厂是 `module.path:factory`。 |
| 当前饱和语义 | ALG-004 限制**单个** abstract intent 的 `abs(u)<=0.10` 与相邻 ordinary 帧 `abs(Delta u)<=2.0*dt`；ALG-006 以 common-`q` 对单轴 normalized request 和三个 synthetic resource 取最小值并保存 signed residual。`FlightCommand` validation 逐载荷校验；底层电机软限位、步长/速度及 fan ramp 是独立最终约束。当前没有双轴 vector envelope。 |
| 可验证的软件分配 | ALG-006 仅有单轴 normalized synthetic allocation；没有 pitch/roll 物理矩阵、roll actuator projection 或验证过的多轴 arbitration。 |
| availability | `valid/fresh/healthy`、`output_known`、`actuation_allowed` 和 owner/readback 是状态/裁决；不存在 runtime availability → numerical authority 的已验证映射。 |
| hardware metadata | `left/right_lift` 与 `left/right_pitch` 是逻辑键；CAN ID、`motor_signs`、软限位、GPIO12/26、PWM 范围描述连接与保护，不证明恢复力矩/推力。 |

依据：[Flight API](../FLIGHT_CONTROL_API.md)、[架构](../FLIGHT_CONTROL_ARCHITECTURE.md)、
[硬件参考](../HARDWARE_REFERENCE.md)、[ALG-006 Task](ALG-006.md)、
[ALG-006 Result](../algorithm_results/ALG-006_CANDIDATE_A.md) 及当前 `core/models.py`、
`core/validation.py`、`runtime/imu_adapter.py`、六个 Candidate、benchmark helpers/tests。

**Non-goals：** 不定义真实 motor/fan mixer、differential fan 的物理 roll 作用、motor rad projection、
fan PWM/推力映射、runtime degradation、真实最大可恢复扰动、实机稳定性或安全包线；不改
Flight API、Runtime、factory、manager、driver、配置映射或底层安全机制。本设计不提前指定
Candidate 的控制律、增益或类结构。

## 3. Preconditions、inputs / outputs 与软件契约

未来 Candidate 仍须通过现有 factory 创建纯 Python `FlightController`，实现 `reset()` 与
`update(FlightState, positive finite dt)`。Runtime 的 state/command validation 保持前置/后置；
算法不读取 ROS/硬件，也不自行获得 authority。普通 Level B 输出仍是完整当前 motor feedback
position hold、左右 fan `0.0`；非零 absolute rad/fan command projection 是单独评审的前置
契约，当前 `NOT DEFINED / NOT VERIFIED`，不能由 task-local intent 猜测。

Level A/D/E 的 **task-local、software-only** 联合观测至少包含同一帧的
`(pitch_request, roll_request)`、`(pitch_served, roll_served)`、signed residual vector、
`status/disposition` 与继承的 process phase/timeout latch。请求和服务量是无量纲 normalized
软件量，不是 `FlightState`/`FlightCommand` 字段或真实 torque/thrust。合法普通请求各分量位于
`[-1,1]`；pitch 归一化仅沿用 ALG-006 的 `u_pitch/0.10`，roll 的 request 来源、角速度/趋势
语义及与 `0.10` 的关系仍是 Candidate 前置设计项，不得直接套用 pitch gain。每帧同步读取两轴，
不得把同一时刻的组合扰动拆成两个相互独立运行的单轴资格结果。

所有普通输出必须 finite、可重复、有界；每轴服务方向不得与该轴合法非零 request 相反，
零请求不能获得虚构的该轴服务量；`residual_i=request_i-served_i` 保留符号。饱和不能绕过
ALG-004 ordinary pitch envelope/slew 或任何 Level B/底层限制。ALG-005 `TIMED_OUT` 的零是
stop sentinel，优先于普通双轴零；精确 safe-stop 且 latch 只能由显式 reset 解除。

## 4. Inherited Requirements

以下门槛须在未来 ALG-007 **最终 ordinary/fail-close 路径**重新执行对应未修改 Profile，
历史 Result 只证明基线，不替代本轮资格。

| 来源 | 必须继承的行为 |
| --- | --- |
| 公共 Flight API / Benchmark Contract | factory 兼容、不可变合法 state、完整合法 command、可变正有限 `dt`、finite、前置 validation layer、无载荷 exact safe-stop、reset、三次可重复、纯软件执行。 |
| ALG-001 Profile v1 | pitch neutral、软件方向、比例/幅值关系、镜像、reset 与 invalid/stale fail-close。 |
| ALG-002 Profile v1 | pitch zero-rate、diverging/recovering 与 signed damping、zero-pitch moving、合法/非法 `dt`。 |
| ALG-003 Profile v1 | pitch 小噪声抑制、有效信号保留、D00A/B/C、history/reset 与 freshness 故障。 |
| ALG-004 Profile v1 | pitch ordinary `abs(u)<=0.10`、`abs(Delta u)<=2.0*dt`、directed transition、饱和、反向及历史清除；这是软件 intent 约束，不是真实 actuator safety。 |
| ALG-005 Profile v1 | phase、episode/dwell timer、`0.300 s` stable dwell、`3.000 s` timeout latch、A/B/D/E 单轴 synthetic recovery、失效及 reset。 |
| ALG-006 Profile v1 | 结构化 timeout/ordinary disposition、单轴 normalized common-`q`、status/residual、非法 authority fail-close、左右镜像及 Level B hold/fan-zero；单轴 seam 在纯 pitch regression 中保持原义。 |

不得通过双轴逻辑修改旧 Profile/fixture/Result 或使已合格算法的 factory 失效。新 roll 处理
必须沿用输入无效、非有限、未知和 reset 后不复用旧命令的 fail-close 原则。

## 5. Multi-axis、耦合、饱和与 arbitration 要求

联合场景须同时改变 pitch/roll，记录逐帧双轴 request、served、residual 与 phase；至少覆盖
同号、反号、大小不等、一个轴先达到限幅及一个轴为零的情况。单轴回归必须保留 ALG-001～006
pitch 语义；roll-only 场景必须有明确 roll 软件方向与中性响应。roll-rate 的未来坐标契约
见 §7；实现及 roll request/control law 仍未完成，不能声称继承了 pitch 的 rate 证据。

资格 fixture 可以定义两个独立轴上限与一个共享的 **synthetic normalized budget**。合法
served vector 必须满足每轴请求/上限及联合预算，residual 显式记录；一轴达到上限但另一轴
仍有软件预算时，不能无理由把另一轴置零。共享预算只是制造竞争的测试约束，不代表电机
或风扇真实 authority。axis caps 与共享预算 `b` **严格只属于 Level A/D/E task-local qualification
fixture 输入**，不是 `FlightState` public fields、runtime hardware authority、measured actuator
capability 或 Candidate configuration。Candidate 不得从 motor/fan health、availability、
`actuation_allowed`、hardware mapping、soft limits、PWM 或 `motor_signs` 推导这些数字。
其具体数值、测试向量与期望关系见 Benchmark §14；任何变化须版本化。当前 Level B 的
nominal axis caps / combined budget 为 **NOT APPLICABLE**；不能由 Level B payload、
`FlightState` 或 runtime 状态推导。未来若有非零 executable projection，须由该独立契约
重新定义软件数值来源和语义，不能表示未经验证的 physical authority。

`b` 与 axis caps 一样必须是非 bool、finite 且位于闭区间 `[0,1]` 的已知软件数字；`None`、
bool、NaN、正负 Inf、负值或大于 `1` 均非法。task-local fixture validation 必须显式拒绝，
或在 task-local seam 进入 `FAIL_CLOSED`；不得 silent clamp、用 tolerance 接受越界，或把未知/
非法 `b` 当作 `0`、`1`。拒绝后不得复用 prior served vector/allocation；若故障到达
controller layer，仍须 exact safe-stop 并清除 ordinary history。

已知合法 zero-capability 与非法输入不同。非零某轴 request 遇到该轴 `cap=0` 时，该轴
`served=0`、signed residual 保留全部该轴 request；另一轴若有合法请求和剩余预算，仍按已
冻结的非竞争关系服务。非零联合 request 遇到合法 `b=0` 时，整个 served vector 为 exact zero，
signed residual 等于 request。两者均不复用旧 allocation，必须可诊断为受影响轴或整个向量的
**INFEASIBLE 语义**；全局 status 及混合场景评分按 Benchmark §14.2 已冻结规则执行。
ordinary zero 才是 `NEUTRAL`；已知正能力不足对应 `SATURATED` 语义；非法输入是
`FAIL_CLOSED`；继承的 `TIMED_OUT` zero 保持独立 stop disposition，五者不得混淆。

已冻结的 arbitration 是先逐轴 cap、再按同一比例缩放以满足 combined L1 budget；
精确公式、零分支、status 优先级和场景期望见 Benchmark §14.2。它是无状态的软件预算规则，
不含 pitch-first、roll-first 或硬件权重。输入校验和 timeout/fault 优先于普通分配；不复用
旧帧。该规则不得绕过继承的 pitch envelope、Level B 或底层限制。

轴耦合只允许 `synthetic / normalized / software stress model`。fixture 应把同一输出向量注入
两个软件状态分量，检验竞争下两个轴不会被彼此遗漏，并记录逐轴状态/趋势和残差；耦合
系数、更新顺序、初态、扰动及 score 见 Benchmark §14.3 的已冻结纯软件 fixture。当前仓库无经过验证的
物理 coupling matrix，故不得把该 fixture 称为 WindArmor 真实动力学。

## 6. Fail-close 与 qualification design

前置 state validation 拒绝 NaN/Inf 或缺失的有效 IMU 分量；task-local fixture/seam 单独校验
axis caps 和 `b`，不得把这些非 `FlightState` 字段冒充 state-validation 输入。
controller-layer 的 stale、unknown、unhealthy、E-STOP 非明确 false、非法 `dt`、roll source
不可用、contract/internal failure 必须返回 exact `FlightCommand.safe_stop()`，清除 ordinary 双轴
历史及 allocation，不借另一轴继续
输出。已 latch 的 timeout 保留至显式 reset。普通双轴零是合法 neutral；timeout zero、fault
zero 和已知 synthetic zero-capability 的 infeasible 诊断不能混淆。故障恢复后的第一普通帧与
fresh cold run 等价；Level B 不从 `actuation_allowed`、motor/fan health 推导数字 authority。

规划中的 Level A/B/D 覆盖双轴静态关系、同步 sequence、耦合压力、共享饱和、reset 和故障；
ALG-001～006 inheritance 原样重跑，ALG-005 Level E 仍仅证明单轴 synthetic pitch。新的多轴
Level E 采用 Benchmark §14.3 的联合评分契约；roll rate 实现和非零投影仍是前置依赖；
**不能用两项单轴 PASS 或只有 A/B/D 的 PASS 代替 M3 多轴动态 qualification**。已存在的软件
容差 `abs_tol=1e-9`、`rel_tol=1e-6` 可用于派生数值比较；exact sentinel、enum、场景计数、
合法边界和 reset 不用 tolerance。ALG-005 的 `dt=0.020 s`、`0.300 s` dwell、`3.000 s` timeout
及 pitch Level E 门槛只在原单轴 fixture 继承，不自动扩展为 roll 的物理或双轴恢复阈值。

正式资格前还必须完成共享 roll-rate 字段及其 singularity rule、非零 projection、roll request
来源和完整 Profile review；针对固定 implementation commit
fresh 运行全部 Hard Gates 与 Software CI，再单独创建 Candidate Result。当前**没有** ALG-007
Formal Qualification PASS。

## 7. Future shared roll-rate API/runtime prerequisite — v0.5.0-020A DESIGN

本节是 020B 之前的长期设计记录。它只冻结 future contract；
`relative_roll_rate_rad_s` 仍为 **PLANNED / NOT IMPLEMENTED**，本工作单元没有修改公共 API、
Runtime、配置或测试。

### 7.1 Current implementation and compatibility audit

当前 `ImuState` 是 `frozen=True` dataclass，字段顺序为 orientation、raw roll/pitch/yaw、relative
roll/pitch、`relative_pitch_rate_rad_s`、angular velocity、linear acceleration、age、
`valid/fresh/connected/zero_generation`；全部字段均无 default，且不存在 roll-rate 字段。仓库内
`ImuState(...)` 直接构造点共 4 个：`ImuAdapter.snapshot()` 的 paired/no-pair 两处 production
构造，以及 `make_fake_flight_state()` / `make_unobserved_flight_state()` 两处 fake helper；均为
keyword construction。仓库测试和文档没有其它直接构造点，但公共模型仍须保护仓库外 positional
caller。

当前 `_validate_imu()` 对所有可选数值先执行 finite 校验；当 `imu.valid=true` 时，orientation、
raw 三轴欧拉角、relative roll/pitch、pitch rate、angular velocity 与 linear acceleration 必须
全部存在，并要求 connected 明确为 true、zero generation 已知、age 已知；`fresh=true` 还要求
`valid=true`。这是当前 base complete measurement set。没有 field-level optional derived
measurement precedent。

`RawImuObservation` 保存规范化 quaternion、由其得到的 raw Euler、同一 raw message 的 body
angular velocity/linear acceleration、派生 pitch rate、source stamp 与接收时刻。`update_raw()`
在写缓存前完成 quaternion、vector、pitch-rate、stamp、重复/乱序校验，任一 source-invalid
条件拒绝整个 update；有效 raw 与 relative attitude 仅按完全相等的正 source stamp 配对。
不匹配的 pending sample 不替换现有 paired snapshot；新完整 pair 才原子替换。freshness 使用
两条消息较早的 monotonic receive time。断线/reconnecting 当前只将 connected 置 false，不清除
pair；zero-generation 改变会清除 pair/pending cache。future roll rate 不得因此复用旧派生值。

ALG-001 只读取 relative pitch；ALG-002～006 读取 relative pitch 与 pitch rate；六个 controller
都依赖 `imu.valid`、`imu.fresh` 和 aggregate freshness，均不读取 roll rate。因此把 roll rate
加入全局 complete measurement set 会让 conditioning-only unavailable frame 收缩全部历史
合格路径的 valid domain，是无必要的 backward-incompatible regression。

### 7.2 Z-Y-X kinematics and conditioning — FORM FROZEN

令同一条 raw IMU sample 的 body angular velocity 为 `(p,q,r)`，规范化 quaternion 按当前
`quaternion_to_euler()` 得到未施加轴向符号的 Z-Y-X `(phi,theta,psi)`：

```text
raw_euler_roll_rate
  = p + sin(phi)*tan(theta)*q + cos(phi)*tan(theta)*r

relative_roll_rate_rad_s
  = roll_axis_sign * raw_euler_roll_rate
```

该公式已用与现有 pitch-rate 测试相同的 quaternion body-rate 微分约定独立作数值导数检查。
把 body rate 到 roll rate 的线性映射写成：

```text
a(phi, theta) = [1, sin(phi)*tan(theta), cos(phi)*tan(theta)]

||a||_2^2
  = 1 + sin(phi)^2*tan(theta)^2 + cos(phi)^2*tan(theta)^2
  = 1 + tan(theta)^2
  = sec(theta)^2

||a(phi, theta)||_2 = 1 / abs(cos(theta))
```

因此 L2 amplification 与 `phi` 无关，并在 `theta -> +/-pi/2` 时无界。这是 Euler coordinate
conditioning，不是 actuator physics、真实恢复能力或硬件安全包络。

现有 `EULER_GIMBAL_LOCK_COS_TOLERANCE=1e-9` 是表示接近数学 gimbal-lock 的 guard，没有证据
证明它限制了可接受 roll-rate amplification。即使 `abs(cos(theta)) > 1e-9`，理论增益仍可任意
接近 `1e9`；所以仅凭现有值宣称 roll rate 数值或语义可接受的答案是 **NO**。

候选无关策略评审：

| 策略 | 020A 结论 |
| --- | --- |
| A. exact-singularity-only | 不控制近奇异点 amplification，不足以冻结 roll-rate availability。 |
| B. `K(theta)<=K_max` | 数学可解释、与硬件 authority 无关，是优先结构；但仓库没有可接受输出误差与 source error bound，无法证明任何具体 `K_max`。 |
| C. `abs(theta)<=theta_max` | 仓库没有 verified mechanical/software attitude envelope；不得发明 `theta_max`。 |
| D. body-frame `p` | 避免 Euler roll-rate 奇异性，但不等同于 `relative_roll_rad` 的时间导数，会改变 v0.5.0-019 coordinate contract；未经 contract revision 不采用。 |

仓库搜索未发现经过冻结/验证的 angular-rate measurement error bound、orientation error bound、
runtime input envelope、mechanical attitude envelope、numerical error budget、public roll-rate magnitude
bound 或 controller-accepted roll-rate error bound。synthetic 小角度 fixture、motor limits、硬件
metadata 与物理直觉均不能填补该证据。因此 **exact numerical near-singularity bound remains
NOT FROZEN**。后续须先提供可审查的 body-rate/orientation 误差上界与允许的 roll-rate 误差预算，
或冻结独立 verified operating envelope；之后才能选择 inclusive/exclusive comparison 与常数。

### 7.3 Public compatibility and fail-close semantics — FROZEN DESIGN

未来字段采用：

```python
relative_roll_rate_rad_s: float | None = None
```

并追加在 `ImuState` 末尾。已有字段全无 default，因此这种 ordering 合法；旧 positional 与
keyword construction 均保持原义，外部 caller 不要求一次性修改。字段是 optional derived
measurement，不加入 legacy/base complete measurement set：

- `imu.valid` 继续表示当前 base IMU pair、连接、zero-generation 和既有完整测量契约有效，
  不代表所有未来 optional derived field 都存在；
- validation 对 roll rate 执行 optional-finite：`None` 合法，finite 合法，NaN/Inf 无论 base
  valid 状态如何均非法；
- source 合法但 conditioning rule 排除时，保留 raw observation/pair，字段为 `None`，不得令
  整体 `imu.valid=false`；
- ALG-007 等需要该字段的 consumer 必须同时要求 `imu.valid`、`imu.fresh`、字段非 `None` 且
  finite，否则 fail-close；ALG-001～006 不因该字段 unavailable 改变行为；
- `None` 精确表示当前 snapshot 没有可用的 roll-rate 派生测量，包括 no pair、conditioning
  exclusion 或 lifecycle invalidation；它不表示零，也不允许补入 prior value；
- stale pair 可以保留属于旧 snapshot 的 finite 值，但 `fresh=false`，consumer 不得把它作为
  fresh trend；disconnect/reconnect 或 zero-generation transition 后，在新的完整 pair 到达前
  roll rate 必须为 `None`，即使为兼容 base pair 的其它字段仍被保留。

不新增 `relative_roll_rate_valid`、`roll_rate_available` 或 reason enum。`None` 加现有
`valid/fresh` 已能无歧义表达 consumer prerequisite；诊断原因可以保留在 Runtime 内部日志/状态，
目前没有无法由该组合表达的公共安全需求。

必须区分：SOURCE INVALID（非法 quaternion、非有限 body rate、非法 stamp、重复/乱序等）继续
拒绝 raw update；DERIVATION UNAVAILABLE 是 source 本身合法但不满足未来 conditioning rule，
必须保存 raw observation，并将 roll rate 置 `None`。后者不能污染或丢弃 base sample。

### 7.4 roll-axis sign, same-sample and lifecycle contract — FROZEN DESIGN

relative attitude producer 已使用 `imu_cybergear_params.yaml` 的 `roll_axis_sign` / `pitch_axis_sign`
（默认均 `1.0`），先对 raw Euler 乘 axis sign，再 normalize，最后减 zero offset 并再次
normalize。共享 motor config parser 先拒绝 bool/非有限值再严格要求 `+1/-1`；实际 relative
observer callback 依赖已声明的 ROS double parameter type，并用 membership 检查 `+1/-1`，其
Python 条件本身没有显式排除 bool。020B 必须使 producer/consumer 的显式校验都拒绝 bool、0、
其它数值及非有限值，并增加对应测试。

Flight Runtime 当前只从 `flight_control.yaml` 读取 `pitch_axis_sign` 并传给 `ImuAdapter`；没有
roll sign。020B 的最小兼容 migration 是在 RuntimeConfig、Flight YAML 与 `ImuAdapter.__init__`
增加同名 `roll_axis_sign`，默认 `1.0`，沿用 producer 已有 key 和严格 `+1.0/-1.0` 校验。Flight
与 producer 仍是两个 node-scoped parameter source，因此必须用 repository configuration test
比较两边明确值，并分别用非对称 signs 验证 roll/pitch 没有 wiring 互换；不能依赖相同 implicit
default。不得从 `motor_signs`、actuator mapping、CAN ID 或 fan mapping 推断。

roll rate 必须由同一 raw message 的规范化 orientation 与 body angular velocity 计算；使用 raw
`phi/theta`，最后才施加 `roll_axis_sign`。不得有限差分 relative angle、不得使用 zero offset、
不得跨帧混合、不得复用上一帧。raw observation 只有与相同 source stamp 的 relative attitude
配对后才进入 snapshot。conditioning-unavailable 的新完整 pair 必须原子替换旧 pair 的 roll-rate
availability；disconnect/reconnecting transition 使旧 roll rate 失效，reconnect 后需新完整 pair；
zero-generation 改变继续清除 pair/pending state。

### 7.5 Future 020B implementation plan — NOT EXECUTED

| Future file | Expected change |
| --- | --- |
| `core/models.py` | 在 `ImuState` 末尾追加 default-`None` optional roll-rate 字段。 |
| `core/validation.py` | 加入 optional-finite 校验；明确排除在 legacy/base complete set 之外。 |
| `runtime/observations.py` | `RawImuObservation` 保存 `float | None` derived roll rate。 |
| `runtime/imu_adapter.py` | 增加 Z-Y-X roll-rate helper、严格 sign、数值 guard、source-invalid/derivation-unavailable 分流及 lifecycle invalidation；不改 pitch-rate 语义。 |
| `runtime/config.py` | RuntimeConfig/default/build validation 增加 `roll_axis_sign`。 |
| `runtime/state_aggregator.py` | 将验证后的 roll/pitch signs 一并传入 adapter。 |
| `runtime/node.py` | 参数声明/读取由 `PARAMETER_DEFAULTS` 自动覆盖，无专用逻辑预计不需修改；实现时以 actual diff 验证。 |
| `config/flight_control.yaml` | 显式增加 `roll_axis_sign: 1.0`，并说明须与 producer 一致。producer YAML 已有同名 key，预计不改其值。 |
| `imu_relative_observer_node.py` | 明确拒绝 bool/nonfinite/非 `+1/-1` sign，消除只依赖 ROS parameter typing 的歧义；不改变合法默认值。 |
| `docs/FLIGHT_CONTROL_API.md` | API implementation 同步记录 optional-derived 与 consumer-specific prerequisite；这是后续 API task，不在 020A 修改。 |
| `docs/FLIGHT_CONTROL_ARCHITECTURE.md` | 同步 Runtime conditioning/lifecycle 设计；这是后续 architecture/API task，不在 020A 修改。 |
| fake builders/fixtures | 默认显式给 finite roll rate；另覆盖旧 constructor 不传新字段仍兼容。 |
| unit/integration tests | 新 helper/adapter/model/validation/runtime config tests；原样重跑 ALG-001～006 与完整 Software CI。 |

helper 名称可采用 `euler_roll_rate_from_body_angular_velocity()` 与
`corrected_relative_roll_rate_rad_s()`，但名称不是 contract。行为必须是 finite source、同一
raw sample、raw `phi/theta`、sign 最后施加、确定输出、conditioning guard、无 angle finite
difference、无 actuator sign、无 prior reuse。

### 7.6 Future 020B acceptance-test matrix — DESIGN ONLY

| # | Scenario | Expected semantics |
| --- | --- | --- |
| 1 | zero orientation + zero rates | finite exact zero |
| 2 | pure `p` at zero attitude | raw roll rate equals `p` |
| 3 | `q` cross-term at nonzero `phi/theta` | matches `sin(phi)*tan(theta)*q` |
| 4 | `r` cross-term at nonzero `phi/theta` | matches `cos(phi)*tan(theta)*r` |
| 5 | mixed `p/q/r` | matches full formula and quaternion numerical derivative |
| 6 | `roll_axis_sign=+1` | corrected rate equals raw rate |
| 7 | `roll_axis_sign=-1` | corrected rate is exact sign inverse |
| 8 | sign bool/0/2/NaN/Inf | rejected before ROS resources/adapter use |
| 9 | non-unit finite quaternion | normalized; result matches equivalent unit quaternion |
| 10 | nonfinite body-rate component | source invalid; raw update rejected |
| 11 | equal raw/relative stamp | one paired snapshot; same raw orientation/rates used |
| 12 | mismatched stamps | no cross-pair; no new roll rate |
| 13 | stale paired sample | value may remain, `fresh=false`, consumer fail-close |
| 14 | disconnected | base invalid/fresh false; roll rate unavailable/no reuse |
| 15 | reconnect | no old roll rate; new complete pair required |
| 16 | zero-generation change | pair/pending/roll rate cleared |
| 17 | duplicate/out-of-order raw | source update rejected; no state corruption |
| 18 | accepted side immediately outside guard | **BLOCKED ON NUMERICAL RULE** |
| 19 | rejected side/on guard boundary | **BLOCKED ON NUMERICAL RULE** |
| 20 | exact gimbal-lock region | unavailable/rejected consistently with frozen source/derivation split; exact boundary blocked on rule |
| 21 | valid pair then conditioning-unavailable pair | new field is `None`; prior rate never reused |
| 22 | three identical runs | bitwise/deterministically identical observations |
| 23 | pitch-rate regression | existing helper/adapter values and sign behavior unchanged |
| 24 | ALG-001～006 regression | unchanged Profiles/tests; optional roll absence does not contract base validity |
| 25 | old `ImuState` construction | old positional/keyword calls succeed and new field defaults `None` |
| 26 | public validation None/finite/NaN/Inf | None + finite accepted; NaN/Inf rejected; field excluded from base complete set |

### 7.7 Design status

| Item | Status |
| --- | --- |
| Z-Y-X roll-rate formula | **FROZEN** |
| Conditioning model `K(theta)=1/abs(cos(theta))` | **FROZEN** |
| Exact near-singularity numerical rule | **NOT FROZEN — EVIDENCE BLOCKER** |
| API compatibility / validation semantics | **FROZEN DESIGN** |
| `roll_axis_sign` wiring | **FROZEN DESIGN** |
| Shared API/runtime implementation | **NOT IMPLEMENTED** |
| Full ALG-007 Profile | **NOT YET FROZEN** |
| ALG-007 Candidate | **BLOCKED / NOT IMPLEMENTED** |

## 8. Executable command prerequisite 与硬件证据边界

`normalized pitch/roll recovery request -> nonzero FlightCommand` 是
**SEPARATE REVIEWED PREREQUISITE / NOT DEFINED / NOT VERIFIED**。当前 Level B 仅有完整
电机当前位置保持和左右 fan `0/0`；软件 request、served 或 residual 不能反推 absolute motor
rad target、fan command、真实恢复方向或相对 authority。该前置契约至少须回答输入坐标/
符号、输出 actuator 字段、绝对值或增量语义、中性点、命令包络、方向证据、资源归属、
validation/freshness、fail-close、reset/history、现有安全机制交互、证据来源及硬件结论边界。
不得在本任务填入未经验证的物理数值。

合成 vector/plant 仅是 software benchmark assumption。motor torque effectiveness、fan
RPM/thrust/direction/moment arm、真实 pitch/roll allocation、motor/fan relative/dynamic
authority、availability → numerical authority、真实 cross-coupling、最大可恢复扰动、
sim-to-real、real actuator safety、硬件动态闭环恢复和 real Balance Recovery 均保持
**UNKNOWN / NOT VERIFIED**。ALG-007 Candidate **BLOCKED / NOT IMPLEMENTED**；Candidate Result
**NOT CREATED**；Formal Qualification **NOT EXECUTED**；hardware access **NO**，authorization
**NONE**，validation **NOT AUTHORIZED / NOT EXECUTED**；v0.5.0 **NOT RELEASED**。

## 9. Remaining review items

1. 先取得并评审可支持 exact near-singularity guard 的误差预算或 verified operating envelope；
   在此 blocker 关闭前不直接进入 020B implementation。
2. blocker 关闭后按 §7.5/§7.6 实现并评审 shared roll-rate API/runtime prerequisite。
3. 独立定义并评审非零 executable `FlightCommand` projection；不得由 hardware metadata 猜测。
4. Candidate 前确定 roll request 的来源、归一化与继承 pitch intent 的兼容关系。
5. 前置依赖完成后 review 完整 ALG-007 Profile；只在固定 implementation SHA 上执行未来
   Formal Qualification。既有 ALG-001～006 Profile、fixture 和 Result 保持原样。

本工作单元只形成候选前文档契约和纯软件分析；不创建 PR、不 merge、不实施 Candidate。
