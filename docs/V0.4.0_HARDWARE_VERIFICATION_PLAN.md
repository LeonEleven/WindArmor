# WindArmor v0.4.0 Hardware Verification Plan

> Status: A0 PASS / A1 PASS / GATE B BASELINE RETRY READY, NOT EXECUTED
>
> 当前正式 stable release 仍为 `v0.3.2`。v0.4.0 Flight takeover 尚未经过
> 真实硬件验证。本文档是未来逐 Gate 执行手册，不构成任何硬件操作授权。

默认硬件状态始终按以下前提处理：

```text
CyberGear motors: OFF
ducted fans / ESCs: OFF
```

每个 Gate、Gate 内每个不同供电或故障场景都必须单独获得用户明确授权。前一项
PASS 不自动授权后一项。所有带电动作还必须先逐项满足根目录 `AGENTS.md` 的十项
授权门槛；任何 FAIL 都立即停止，不自动恢复、继续或切换 legacy owner。

## 1. Four-gate readiness

| Gate | 目的 | 当前状态 | 动力边界 |
|---|---|---|---|
| A — Physical + Powered Read-only | 物理检查、IMU 和 passive motor feedback | A0 PASS / A1 PASS / PASSIVE ZERO-TX NOT SUPPORTED, NOT REQUIRED | A0 全断电；A1 只给 IMU/逻辑供电；历史 passive observation 时 motor bus 通电、fan/ESC 断电 |
| B — Bounded Actuator Verification | 先重试 normal feedback baseline，再进行 combined ownership bounded actuation | TASK 6.2.2 SOFTWARE READY / BASELINE RETRY NOT EXECUTED / NOT AUTHORIZED | baseline/B1 motor 通电、fan/ESC 断电；B2 motor 与 fan/ESC 均通电 |
| C — Fail-closed Verification | safe-stop、timeout、Runtime/owner loss、E-STOP | PLANNED / NOT AUTHORIZED | 按子场景单独声明，通常 motor 与 fan/ESC 均通电 |
| D — Legacy + Final RC Regression | legacy 模式与最终正常功能回归 | PLANNED / NOT AUTHORIZED | motor 与 fan/ESC 均通电 |

旧 Stage 0–1 对应 Gate A，Stage 3–5 对应 Gate B，Stage 6–7 对应 Gate C，
Stage 8–9 对应 Gate D。旧 Stage 2 的 reserve pause 不再作为 production feature 或
独立硬件 Gate；现有 Runtime 的 reserve、commit、atomic ownership 和 rollback 在
Gate B/C 中按正式路径一起验证。

## 2. Common contract

### 2.1 Evidence and software gate

结果只允许 `PASS / FAIL / NOT VERIFIED`。每个子场景记录：日期、操作人和安全
观察员、Git HEAD/status、配置文件 hash、授权记录、各电源状态、authority epoch、
generation、两路 owner token/source sequence、state/command sequence、预期、实测、
停止原因、退出后安全状态及日志位置。

每次硬件 session 前先在 actuator 动力断开、且不启动任何硬件 node/launch 时执行：

```bash
cd ~/workspace/WindArmor
git rev-parse HEAD
git status --short --branch
source /opt/ros/jazzy/setup.bash
./scripts/ci_software.sh
```

CI 必须 green，且执行基线必须与记录一致。CI 是纯软件证据，不是实机 PASS。

### 2.2 Fixed configuration facts

- motor logical names：`left_lift, left_pitch, right_pitch, right_lift`；
- CAN ID 映射：`4, 3, 2, 1`；算法和人工命令只使用 logical name；
- `motor_signs`：`[-1.0, 1.0, -1.0, 1.0]`；
- motor limits：`[-1.57,-1.57,-1.57,0.0]` 到 `[0.0,1.57,1.57,1.57]`；
- `motor_feedback_timeout_sec=0.0`，不得为验证而改变；
- fan GPIO：left GPIO12、right GPIO13；首次带电前必须物理确认 GPIO13；
- `flight_fan_max_pwm_us=1400`，normalized command 不是 thrust fraction；
- `flight_takeover_enabled=false`、`verification_controller_enabled=false`；
- `motor_test_offset_configured=false`，仓库没有获准的真实 offset；
- fan test command 默认 `0.0 / 0.0`。

Gate B/C 使用 controller factory：

```text
windarmor_flight_control.algorithms.bounded_verification_controller:create_controller
```

执行前必须替换下列占位符；任何占位符仍存在时禁止启动：

```text
<APPROVED_LOGICAL_MOTOR>
<APPROVED_OFFSET_RAD>
<APPROVED_FAN_LEFT_COMMAND>
<APPROVED_FAN_RIGHT_COMMAND>
```

### 2.3 Global immediate-stop conditions

- logical motor、CAN ID、实际轴或方向不一致；
- 非批准轴运动、目标跳向零位、超出批准 offset 或接近机械干涉；
- fan 错侧、异常方向、非批准旋转或超过 Flight PWM 上限；
- fault bit、临界温度、CAN/serial 异常、owner/token/sequence 不一致；
- unexpected ACTIVE、旧 epoch/generation/target 重现或 legacy 自动恢复；
- E-STOP、watchdog、command lease 或 safe-stop 没有 fail-closed；
- 异常声音、振动、气味、温升、结构松动或日志丢失；
- 操作人或现场安全观察员要求停止。

立即发布系统急停（若 ROS 仍可用）：

```bash
ros2 topic pub --once /e_stop std_msgs/msg/Bool "{data: true}"
```

同时准备直接切断对应 actuator 动力。不得把 ROS 命令作为物理断电的替代。

## 3. Gate A — Physical + Powered Read-only

当前执行记录：A0、A1 已由用户完成并报告 PASS。Task 6.2.1 修复 Jazzy launch
兼容性后，用户另行执行 passive zero-TX observation：transport connected，但四台
CyberGear 均持续 `has_feedback=false`。因此 passive zero-TX feedback 记为
`NOT SUPPORTED BY OBSERVED HARDWARE BEHAVIOR / NOT A RELEASE REQUIREMENT`，不再要求
retry，也不把缺失 frame 伪造成硬件 PASS。

### A0 — Power-off physical inspection

【本阶段需要通电】

- 无。

【本阶段必须保持断电】

- CyberGear motor bus；
- fan/ESC；
- 检查接线时 Raspberry Pi/逻辑电源也断开。

【上电前检查】

1. 确认全部电源确实断开并完成放电；机器人固定，机械范围内无人。
2. 核对 CAN H/L/GND、终端电阻、CAN ID 4/3/2/1 与四个 logical motor。
3. 核对 IMU X+ 向前、Y+ 向左、Z+ 向上。
4. 核对 GPIO12/pin32 为左 fan、GPIO13/pin33 为右 fan、GND 和 ESC 极性。
5. 手动缓慢确认机械净空、线缆余量、软限位与机构范围，并确认 motor/fan 可独立断电。

【Terminal 1】

执行：

```bash
cd ~/workspace/WindArmor
git rev-parse HEAD
git status --short --branch
sed -n '1,360p' src/imu_cybergear_ros2/config/imu_cybergear_params.yaml
sed -n '1,220p' src/windarmor_fan_controller/config/fan_params.yaml
sed -n '1,220p' src/windarmor_flight_control/config/flight_control.yaml
```

预期：只读出当前基线；无 ROS node、CAN、串口或 GPIO 访问。

【物理预期】

应该发生：只有断电目视、标签和缓慢手动净空检查。

绝对不应该发生：上电、保持力、PWM/ESC 声音、电机或 fan 主动运动。

【PASS】所有映射、接线、极性、机械净空和物理断电手段都有证据；无法在断电下
确认的动态方向记 `NOT VERIFIED`，不伪造 PASS。

【FAIL / 立即停止】任何接线身份、极性、GND、机械范围或安全断电方式不明确。

【安全退出】保持所有动力断开，保存照片和检查记录；FAIL 项修复前不进入 A1/A2。

### A1 — Sensor / IMU observation

【本阶段需要通电】

- Raspberry Pi 与 IMU 所需逻辑电源。

【本阶段必须保持断电】

- CyberGear motor bus；
- fan/ESC。

【上电前检查】完成 A0、software gate green、IMU 固定牢靠且姿态动作可由人工小幅完成。

【Terminal 1】

```bash
cd ~/workspace/WindArmor
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch imu_cybergear_ros2 imu_cybergear_system.launch.py \
  start_controller:=false start_rviz:=false
```

预期：只启动 IMU driver；明确会访问 `/dev/imu_usb`，不创建 motor controller。

【Terminal 2】

```bash
source /opt/ros/jazzy/setup.bash
source ~/workspace/WindArmor/install/setup.bash
ros2 topic echo /imu/data_raw sensor_msgs/msg/Imu
```

【Terminal 3】

```bash
source /opt/ros/jazzy/setup.bash
source ~/workspace/WindArmor/install/setup.bash
ros2 topic echo /imu/status std_msgs/msg/String
```

预期 ROS 输出：`/imu/data_raw` 连续合法 quaternion/velocity/acceleration，status 明确
connected；物理小幅 roll/pitch 与坐标定义一致。

【物理预期】应该发生：手动改变机体姿态时读数相应变化。绝对不应该发生：任何
motor/fan 动作、CAN 初始化或 GPIO/PWM 输出。

【PASS】连接与数据新鲜，轴和符号与物理方向一致。【FAIL / 立即停止】无数据、无效
quaternion、轴/符号错误、出现 actuator node 或动作。

【安全退出】Terminal 1 `Ctrl+C`，确认 IMU node 退出，再断逻辑电源；motor/fan 继续断电。

### A2 — Motor passive observation（历史结果；非 release requirement）

【本阶段需要通电】

- Raspberry Pi、IMU/CAN HAT 逻辑电源；
- CyberGear motor bus。此子阶段明确需要给 CyberGear motor bus 通电。

【本阶段必须保持断电】

- fan/ESC。

【上电前检查】A0/A1 PASS；机器人固定；所有 motor 有机械净空；physical motor kill
可立即操作；确认 graph 中无普通 motor controller、fan controller 或旧 Runtime。

【Terminal 1】

```bash
cd ~/workspace/WindArmor
sudo ./scripts/setup_can.sh can10
ip -details link show can10
```

预期：`can10` 为 UP、1 Mbps。此系统配置命令只能在本阶段获授权后执行。

【Terminal 2】

```bash
cd ~/workspace/WindArmor
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch windarmor_bringup windarmor_observation_only.launch.py
```

预期：启动 IMU RX、relative observer、motor passive-RX observer 和 takeover 固定为
false 的 Flight Runtime；不初始化 CyberGear actuator，不启动 fan/GPIO。

【Terminal 3】

逐条执行并分别记录：

```bash
ros2 topic echo /motors/observer_status std_msgs/msg/String
ros2 topic echo /motors/feedback windarmor_interfaces/msg/MotorFeedbackArray
ros2 topic echo /imu/relative_roll_pitch geometry_msgs/msg/Vector3Stamped
ros2 topic type /flight_control/dry_run/status
ros2 topic echo /flight_control/dry_run/status
ros2 topic echo /flight_control/authority/status windarmor_interfaces/msg/FlightAuthorityStatus
ros2 node list
ros2 topic list
ros2 service list
```

已观测 ROS 结果：transport connected；四 logical motor 都出现，但均持续
`has_feedback=false/valid=false/fresh=false`。这确认 passive zero-TX 不能作为 Flight
feedback 来源；observer 没有 authoritative control safety latch，fan state 保持 unknown，
也不因此扩展 observer architecture。

【物理预期】应该发生：motor 始终静止。绝对不应该发生：enable、set-zero、目标写入、
保持位置变化、fan PWM 或旋转。

【历史结论】零 TX 无 passive feedback：`NOT SUPPORTED / NOT REQUIRED`。任何 movement、
actuator initialization、unexpected command publisher 或映射错误仍是 FAIL/立即停止。

【安全退出】Terminal 2 `Ctrl+C`；确认 observer 关闭 reader 后先断 motor bus，再按现场
顺序断逻辑电源；fan/ESC 全程保持断电。

### A3 — Fan read-only boundary

当前没有真实 fan RPM/thrust/ESC feedback。Gate A 中 fan/ESC 保持断电，不启动
`fan_controller`，不把缺失状态伪装成 stop measurement。A3 PASS 只表示该 unknown
边界被正确保留，不表示 fan hardware 已验证。

## 4. Gate B — Bounded Actuator Verification

Gate B 首次 normal-controller baseline 已确认：四台 CyberGear initialize PASS、
`MANUAL_RUNNING` PASS、既有 hold 行为正常，但 `/motors/feedback` 持续全部 false，
因此 baseline 暂停。Task 6.2.2 已完成 software-only 修复：configure 阶段 type-2 不再
丢失，active normal owner 以同值 authoritative `loc_ref` 持续取得 type-2；fake tests
验证 target/mode/ownership 不变及 fail-closed lifecycle。当前状态是：

```text
Gate B baseline: PAUSED
After Task 6.2.2: READY FOR SEPARATE HARDWARE RETRY / NOT EXECUTED
```

retry 不由本文档或软件 PASS 自动授权，执行前仍需重新满足十项带电门槛。

Gate B 保持现有 motor+fan atomic ownership，只允许一个域产生非停止动作。
`bounded_verification_controller` 从当前 ACTIVE authority session 的 fresh、valid、
healthy `MotorState.position_rad` 捕获一次 baseline；selected motor 每 tick固定为
`baseline + approved offset`，其他 motor 固定 hold baseline，不逐 tick 累加。

注意：现有 normal motor controller configure 会写入已标定的 `0.0` 初始化目标。
因此启动前必须确认机构位于既有已验证零位并允许该初始化；verification controller
只保证 Flight takeover 后不把零当作新机械中位，不能消除 lower-level 初始化行为。

### B1 — Motor bounded verification

【本阶段需要通电】Raspberry Pi/IMU/CAN、CyberGear motor bus；fan manager/controller
需要逻辑运行以满足 combined ownership。

【本阶段必须保持断电】fan/ESC 动力；若电气结构不能独立断开 fan/ESC，STOP 并重新
评审，不得假设可安全执行。

【上电前检查】Gate A PASS；唯一 logical motor、方向、`<APPROVED_OFFSET_RAD>`、初始
位置、软限位余量、持续时间和 physical kill 已由用户确认；robot 固定；所有占位符
替换；offset 必须非零、有限且不会越过软限位。首次只允许一个 axis。

【Terminal 1】

```bash
cd ~/workspace/WindArmor
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch windarmor_bringup windarmor.launch.py \
  enable_motor_keyboard:=false
```

预期：normal motor/fan safety owners 启动；fan GPIO 输出 stop，但 ESC 动力断开；电机
完成既有初始化并停在确认的零位。任何意外初始化运动立即停止。

【Terminal 2】

仅在替换占位符并再次获准后执行：

```bash
cd ~/workspace/WindArmor
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 run windarmor_flight_control flight_control_runtime_node --ros-args \
  --params-file src/windarmor_flight_control/config/flight_control.yaml \
  -p controller_factory:=windarmor_flight_control.algorithms.bounded_verification_controller:create_controller \
  -p verification_controller_enabled:=true \
  -p test_motor_name:=<APPROVED_LOGICAL_MOTOR> \
  -p motor_test_offset_configured:=true \
  -p motor_test_offset_rad:=<APPROVED_OFFSET_RAD> \
  -p fan_left_test_command:=0.0 \
  -p fan_right_test_command:=0.0 \
  -p flight_takeover_enabled:=true
```

未替换的命令无效且禁止执行。Runtime 启动后不会自行 prepare。

【Terminal 3】

```bash
ros2 topic echo /motors/feedback windarmor_interfaces/msg/MotorFeedbackArray
ros2 topic echo /motors/ownership_state windarmor_interfaces/msg/OwnershipState
ros2 topic echo /fans/ownership_state windarmor_interfaces/msg/OwnershipState
ros2 topic echo /flight_control/authority/status windarmor_interfaces/msg/FlightAuthorityStatus
ros2 service call /flight_control/authority/prepare std_srvs/srv/Trigger "{}"
```

每条持续 `echo` 应使用独立观察终端或记录工具；最后一条 prepare 仅在前述状态全部
fresh/healthy、安全观察员就位后执行。

预期 ROS 输出：两 owner 经 reserve/commit 后使用同一 epoch/generation；Runtime
atomic ACTIVE；首个 command state sequence 大于 cutoff；完整四 motor frame；selected
target=`captured baseline + offset`，其他 target=`captured baseline`；fan=`0.0/0.0`。

【物理预期】应该发生：只有批准 axis 按批准方向作 bounded movement，其他 motor
保持，fan 静止。绝对不应该发生：第二轴运动、逐 tick 漂移、目标跳到零、fan 起转。

【PASS】轴、方向、bounded target、完整 frame、hold、stop fan、revoke 均有证据。
【FAIL / 立即停止】任何未批准动作、drift、fault/温升、owner/sequence 错误。

【安全退出】先发布 `/e_stop=true`；确认 motor halt、fan stop、Runtime INHIBITED 和
owner 最终 NONE；Terminal 2/1 分别 `Ctrl+C`；先断 motor bus，再断逻辑电源。不得
自动 reclaim legacy。

### B2 — Fan bounded verification

【本阶段需要通电】Raspberry Pi/IMU/CAN、CyberGear motor bus、指定 fan/ESC 动力。

【本阶段必须保持断电】未测试 fan 通道如电气结构允许独立断电；否则明确记录两路
均上电，但未测试通道 command 必须为 `0.0`。

【上电前检查】B1 PASS；fan guard、GPIO12/13 和左右映射已确认；批准唯一 fan channel、
`<APPROVED_FAN_*_COMMAND>`、PWM 上限和持续时间；motor baseline/soft-limit margin
健康；不沿用 B1 授权。

【Terminal 1】与 B1 相同启动 normal system；先保持 fan/ESC 断电，确认 stop PWM 后再
按 ESC 既有安全顺序给本阶段批准的 fan/ESC 通电。

【Terminal 2】与 B1 相同，但 motor offset 必须由用户明确设为当次批准值；若本次只
允许 hold，可将明确批准的 offset 设为 `0.0`，仍必须保留
`motor_test_offset_configured:=true`。fan 参数只能一侧非零：

```bash
ros2 run windarmor_flight_control flight_control_runtime_node --ros-args \
  --params-file src/windarmor_flight_control/config/flight_control.yaml \
  -p controller_factory:=windarmor_flight_control.algorithms.bounded_verification_controller:create_controller \
  -p verification_controller_enabled:=true \
  -p test_motor_name:=<APPROVED_LOGICAL_MOTOR> \
  -p motor_test_offset_configured:=true \
  -p motor_test_offset_rad:=0.0 \
  -p fan_left_test_command:=<APPROVED_FAN_LEFT_COMMAND> \
  -p fan_right_test_command:=<APPROVED_FAN_RIGHT_COMMAND> \
  -p flight_takeover_enabled:=true
```

【Terminal 3】

```bash
ros2 topic echo /fans/status_pwm std_msgs/msg/Int32MultiArray
ros2 topic echo /fans/control_state std_msgs/msg/String
ros2 topic echo /flight_control/authority/status windarmor_interfaces/msg/FlightAuthorityStatus
ros2 service call /flight_control/authority/prepare std_srvs/srv/Trigger "{}"
```

预期 ROS 输出：combined owner ACTIVE；motor complete frame 等于 captured baseline；
批准 fan normalized command 经既有 mapping/ramp 输出且不超过 1400 μs；另一侧 stop。

【物理预期】应该发生：仅批准 fan 有 bounded 起转/响应，motor 不动。绝对不应该
发生：motor movement、错侧 fan、直接跳 1.0、把 normalized value解释成 thrust。

【PASS/FAIL/立即停止】正确侧、bounded ramp、stop/revoke 全部符合为 PASS；错侧、异常
振动/声音/气味、motor movement、PWM/owner 异常立即 FAIL 并停止。

【安全退出】`/e_stop=true`，确认 `[800,800]` 和 motor halt；先断 fan/ESC，再退出
Runtime/system，再断 motor bus和逻辑电源。

## 5. Gate C — Fail-closed Verification

Gate C 只在 Gate B PASS 后逐项授权；一次只注入一个条件。初始 command 必须沿用
当次重新批准的 B1/B2 bounded 值，不自动沿用旧 session。每项预期均为 motor halt、
fan stop、Runtime INHIBITED、owner NONE、无 automatic legacy recovery。

### C1 — Algorithm safe-stop from stale required input

【本阶段硬件状态】按当次批准的 B1 或 B2 供电；未包含的 actuator 保持断电。

【Terminal 1/2】按对应 B1/B2 exact command 启动并完成 ACTIVE。

【Terminal 3】使 IMU lifecycle 正常 deactivate，从而停止新 IMU sample：

```bash
ros2 lifecycle set /imu_driver_node deactivate
```

预期：输入超过 `flight_imu_freshness_sec=0.2` 后 controller 返回
`FlightCommand.safe_stop()`；Runtime 不重发旧 target，撤销 combined owner并 inhibit。
物理上 motor halt、fan stop。绝对不应在 IMU 恢复后自动 ACTIVE。

PASS：safe-stop、revoke、无旧目标恢复都有证据。FAIL/立即停止：任何 actuator 继续、
旧 frame 重发或 authority 自动恢复。

安全退出：发布 `/e_stop=true`，确认停止；不重新 activate IMU 来恢复 Flight；退出
Runtime/system并按当前场景先 fan、后 motor 的顺序断电。

### C2 — Command timeout / Runtime unresponsive

【本阶段硬件状态】按单独批准的低幅 Gate B 场景供电。

【Terminal 1】保持 normal hardware system 与状态观察。

【Terminal 2】按 Gate B 命令前台运行 Runtime；ACTIVE 稳定后按 `Ctrl+Z` 暂停该进程。

预期：Runtime 停止发 envelope；motor/fan 各自的 `0.25 s` active command lease 到期，
fail-closed 并释放 owner。绝对不应该继续运动或切回 legacy。

【Terminal 3】

```bash
ros2 topic echo /motors/ownership_state windarmor_interfaces/msg/OwnershipState
ros2 topic echo /fans/ownership_state windarmor_interfaces/msg/OwnershipState
```

PASS：两路 timeout、halt/stop、owner release。FAIL/立即停止：任一路继续输出、owner
残留或自动恢复。

安全退出：先物理断开 actuator 动力；Terminal 2 执行 `kill %1` 终止已暂停 job，禁止
`fg` 恢复旧 Runtime；再退出 Terminal 1。

### C3 — Graceful Runtime stop/restart and stale authority

【本阶段硬件状态】按单独批准的低幅 Gate B 场景供电。

ACTIVE 后在 Runtime 终端按 `Ctrl+C`。预期本地 executable gate 关闭、best-effort
revoke、owner lease 后盾有效，motor halt、fan stop。用原 Gate B 命令重新启动 Runtime
时必须得到新的 authority epoch，状态为 DRY_RUN/NONE；不得自动 prepare 或接受旧
epoch/generation。只有另一次授权才可调用 prepare。

PASS：shutdown/restart 隔离成立。FAIL/立即停止：restart 后 ACTIVE、旧 token/target
恢复或 actuator movement。安全退出：`/e_stop=true`、确认停止、退出所有进程并断电。

### C4 — Owner loss and E-STOP interaction

每个子场景单独授权，不连续执行。

Owner loss 使用正常 lifecycle transition：

```bash
ros2 lifecycle set /imu_motor_controller_node deactivate
```

预期 motor owner/state 消失或释放、motor halt，Runtime 检测 mismatch 并 inhibit，fan
stop/revoke。恢复 lifecycle 不得恢复旧 Flight。

E-STOP 使用：

```bash
ros2 topic pub --once /e_stop std_msgs/msg/Bool "{data: true}"
```

预期 lower-level E-STOP 优先，Flight authority 失效。`/e_stop=false`、transport reconnect
或 Runtime restart 都不得自动恢复 Flight。真实 destructive fault、堵转、过温、过流、
带电拔线不属于正常 RC，禁止执行。

PASS：owner loss/E-STOP 分别 fail-closed且不自动恢复。FAIL/立即停止：任一 actuator
继续或恢复旧 authority。安全退出：保持 E-STOP latch，先断 fan/ESC、后断 motor bus，
再退出进程；恢复命令必须留到新的独立授权。

## 6. Gate D — Legacy + Final RC Regression

【本阶段需要通电】Raspberry Pi/IMU/CAN、CyberGear motor bus、双 fan/ESC。

【本阶段必须保持断电】无，但首次上电仍按 motor 与 fan 分步完成，不能一次授权推定
全部已安全。

【上电前检查】Gate A–C 全部 PASS；Flight Runtime 已退出、两 owner 为 NONE；robot
固定、fan guard/净空、E-STOP/physical kill就绪；既有零点、方向和低幅动作获准。

【Terminal 1】

```bash
cd ~/workspace/WindArmor
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch windarmor_bringup windarmor.launch.py
```

预期：系统按 v0.3.2 legacy 默认启动，motor MANUAL，fan 无 MANUAL/AUTO 授权且 stop；
不得存在 Flight Runtime 或 `/flight_control/command` publisher。

【Terminal 2】

```bash
ros2 topic echo /motors/control_mode std_msgs/msg/String
ros2 topic echo /fans/control_state std_msgs/msg/String
ros2 topic echo /fans/status_pwm std_msgs/msg/Int32MultiArray
ros2 topic echo /motor/status std_msgs/msg/String
```

【Terminal 3】按每项单独批准顺序执行：

```bash
ros2 service call /imu/set_zero std_srvs/srv/Trigger "{}"
ros2 service call /motors/set_zero std_srvs/srv/Trigger "{}"
ros2 service call /fans/manual_enable std_srvs/srv/SetBool "{data: true}"
ros2 topic pub --once /fans/pwm std_msgs/msg/Int32MultiArray "{data: [800, 800]}"
ros2 service call /fans/auto_enable std_srvs/srv/SetBool "{data: true}"
ros2 topic pub --once /e_stop std_msgs/msg/Bool "{data: true}"
```

MANUAL motor、HOME、LEGACY_AUTO 通过既有 motor keyboard 分别作用户批准的小幅测试；
fan MANUAL 必须先建立本次授权后的 `[800,800]` baseline；fan AUTO 只在 motor AUTO、
状态新鲜且本次显式授权后验证。不要同时运行互相竞争的发布者。

预期物理行为：MANUAL/HOME/LEGACY_AUTO 方向与既有契约一致；fan左右映射、MANUAL/
AUTO、ramp 和 stop 正常；E-STOP 同时停止全部 actuator。绝对不应该发生：Flight
owner/command、旧 Flight target、E-STOP clear 后自动动作、ERROR/reconnect 自动恢复。

确认 E-STOP 原因已排除且全部设备静止后，恢复本身需要单独授权，并逐条观察状态：

```bash
ros2 topic pub --once /e_stop std_msgs/msg/Bool "{data: false}"
ros2 service call /enable_motor std_srvs/srv/SetBool "{data: true}"
ros2 service call /fans/enable std_srvs/srv/SetBool "{data: true}"
ros2 service call /fans/reset_e_stop std_srvs/srv/Trigger "{}"
ros2 service call /fans/manual_enable std_srvs/srv/SetBool "{data: true}"
ros2 topic pub --once /fans/pwm std_msgs/msg/Int32MultiArray "{data: [800, 800]}"
```

恢复后 motor 只到 MANUAL，fan 只到本次 neutral baseline；不得自动进入 AUTO 或恢复
旧 PWM。restart 测试必须先按安全退出顺序停止并重新执行 Terminal 1 命令，启动后
再次确认 MANUAL、fan disarmed/stop 和无 Flight owner。

【PASS】依次覆盖 motor MANUAL、LEGACY_AUTO、HOME，fan MANUAL、LEGACY_AUTO，
E-STOP、shutdown、restart、explicit legacy reclaim 和 final normal regression；每项
都有实测证据且无回归。任一项 FAIL 或 NOT VERIFIED 都不得进入 RC/release。

【FAIL / 立即停止】任何模式/方向/通道错误、异常动作、安全机制退化、旧 Flight
authority 恢复或 shutdown 后输出残留。

【安全退出】

1. 发布 `/e_stop=true` 并确认 motor `EMERGENCY_STOP`、fan `[800,800]`；
2. 先断 fan/ESC 动力，再断 motor bus；
3. Terminal 1 按 `q` 或 `Ctrl+C`，确认全部 node 退出；
4. 最后断逻辑电源并保存日志、配置 snapshot 和 PASS/FAIL 记录。

Gate D 全部 PASS 后才允许进入 v0.4.0 RC/release 收口；这仍不自动授权 commit、push、
tag 或 GitHub Release。

## 7. Current software readiness and unresolved execution values

专用 verification controller、配置校验和 pure tests 已准备；它不新增 authority、
ownership、lease 或自动 test sequence。所有 Gate 的 repository interface 名称已按当前
source/config/launch/message/service 核对。

下列值必须在对应 Gate 执行前由用户明确决定，当前保持 `TO BE SET BEFORE EXECUTION`：

- B1 唯一 test motor logical name；
- B1 motor offset rad、方向、初始位置、持续时间；
- B2 test motor hold 选择；
- B2 左/右 fan normalized command、通道、持续时间；
- Gate C 每个故障场景的初始 bounded command 和最大持续时间。

没有这些值不构成软件 blocker，但对应硬件命令不可执行。若真实验证暴露 bug，立即
停止硬件验证并创建只针对该缺陷的修复任务；不继续预先扩展 verification framework。
