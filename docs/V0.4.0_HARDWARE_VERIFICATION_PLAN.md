# WindArmor v0.4.0 Hardware Verification Plan

> Status: A0 PASS / A1 PASS / GATE B FEEDBACK BASELINE PASS /
> B0 COLD-START HOLD PASS / FAN PASSIVE STARTUP SOFTWARE PASS /
> FAN E-STOP PRESERVATION SOFTWARE PASS /
> FLIGHT FAN STATE CONTRACT SOFTWARE PASS /
> B1 FUNCTIONAL HARDWARE PASS /
> B1 <=3 SEC TIMING PENDING GATE C / C4B /
> B2 HARDWARE PASS /
> GATE B COMPLETE /
> TASK 6.2.7 SOFTWARE PASS /
> GATE C / C1 HARDWARE PASS /
> GATE C / C2 HARDWARE PASS /
> GATE C / C3 HARDWARE PASS /
> GATE C / C4 NOT AUTHORIZED / NOT EXECUTED /
> IN PROGRESS / NOT COMPLETE
>
> 当前正式 stable release 仍为 `v0.3.2`。v0.4.0 Flight takeover 已完成 B1 bounded
> motor 和 B2 bounded fan hardware verification，Gate B 已完成；Gate C 的 C1、C2 已通过，
> C3 final powered retry 也已取得 `HARDWARE PASS`；C4 和 Gate D 尚未完成。本文档是未来逐
> Gate 执行手册，不构成任何硬件操作授权。

默认硬件状态始终按以下前提处理：

```text
CyberGear motors: OFF
ducted fans / ESCs: OFF
```

每个 Gate、Gate 内每个不同供电或故障场景都必须单独获得用户明确授权。前一项
PASS 不自动授权后一项。所有带电动作还必须先逐项满足根目录 `AGENTS.md` 的十项
授权门槛；任何 FAIL 都立即停止，不自动恢复、继续或切换 legacy owner。

## 1. Four-gate readiness

| Gate | 目的 | 当前状态 | 动力边界 |
|---|---|---|---|
| A — Physical + Powered Read-only | 物理检查、IMU 和 passive motor feedback | A0 PASS / A1 PASS / PASSIVE ZERO-TX NOT SUPPORTED, NOT REQUIRED | A0 全断电；A1 只给 IMU/逻辑供电；历史 passive observation 时 motor bus 通电、fan/ESC 断电 |
| B — Bounded Actuator Verification | 先验证 cold-start hold 与显式 set-zero，再进行 combined ownership bounded actuation | FEEDBACK BASELINE PASS / B0 PASS / B1 FUNCTIONAL HARDWARE PASS / B2 HARDWARE PASS / GATE B COMPLETE / B1 <=3 SEC TIMING PENDING C4B | B0/B1 motor 通电、fan/ESC 断电；B2 最终 session 为 motor 与 LEFT fan/ESC 通电，RIGHT ESC 独立断电 |
| C — Fail-closed Verification | safe-stop、timeout、Runtime/owner loss、E-STOP | C1 HARDWARE PASS / C2 HARDWARE PASS / C3 HARDWARE PASS / C4 NOT AUTHORIZED, NOT EXECUTED / IN PROGRESS / NOT COMPLETE | 按子场景单独声明，通常 motor 与 fan/ESC 均通电 |
| D — Legacy + Final RC Regression | legacy 模式与最终正常功能回归 | PLANNED / NOT AUTHORIZED | motor 与 fan/ESC 均通电 |

旧 Stage 0–1 对应 Gate A，Stage 3–5 对应 Gate B，Stage 6–7 对应 Gate C，
Stage 8–9 对应 Gate D。旧 Stage 2 的 reserve pause 不再作为 production feature 或
独立硬件 Gate；现有 Runtime 的 reserve、commit、atomic ownership 和 rollback 在
Gate B/C 中按正式路径一起验证。

## 2. Common contract

### 2.1 Evidence and software gate

结果只允许 `PASS / FAIL / NOT VERIFIED`。每个子场景记录：日期、操作人和安全
观察员、Git HEAD/status、配置文件 hash、授权记录、各电源状态、authority epoch、
generation、两路 owner token/source sequence、state/command sequence、预期、实测、
停止原因、退出后安全状态及日志位置。

每次硬件 session 前先在 actuator 动力断开、且不启动任何硬件 node/launch 时执行：

```bash
cd ~/workspace/WindArmor
git rev-parse HEAD
git status --short --branch
source /opt/ros/jazzy/setup.bash
./scripts/ci_software.sh
```

CI 必须 green，且执行基线必须与记录一致。CI 是纯软件证据，不是实机 PASS。

### 2.2 Fixed configuration facts

- motor logical names：`left_lift, left_pitch, right_pitch, right_lift`；
- CAN ID 映射：`4, 3, 2, 1`；算法和人工命令只使用 logical name；
- `motor_signs`：`[-1.0, 1.0, -1.0, 1.0]`；
- motor limits：`[-1.57,-1.57,-1.57,0.0]` 到 `[0.0,1.57,1.57,1.57]`；
- `motor_feedback_timeout_sec=0.0`，不得为验证而改变；
- fan GPIO：left GPIO12、right GPIO26；首次带电前必须物理确认 GPIO26；GPIO13
  保留给 Waveshare 2-CH CAN HAT+ 的 CAN_1 INT_1，不再用于右 fan；
- `flight_fan_max_pwm_us=1400`，normalized command 不是 thrust fraction；
- `flight_takeover_enabled=false`、`verification_controller_enabled=false`；
- `motor_test_offset_configured=false`，仓库没有获准的真实 offset；
- fan test command 默认 `0.0 / 0.0`。

Gate B/C 使用 controller factory：

```text
windarmor_flight_control.algorithms.bounded_verification_controller:create_controller
```

执行前必须替换下列占位符；任何占位符仍存在时禁止启动：

```text
<APPROVED_LOGICAL_MOTOR>
<APPROVED_OFFSET_RAD>
<APPROVED_FAN_LEFT_COMMAND>
<APPROVED_FAN_RIGHT_COMMAND>
```

### 2.3 Continuous evidence workflow

后续瞬态硬件验证统一使用以下角色分工；每个硬件场景仍必须先获得单独授权：

```text
Terminal A: normal bringup
Terminal B: Flight Runtime
Terminal C: record_gate_evidence.py
Terminal D: gate-specific trigger / watchdog
Operator:   physical observation
End:        Ctrl+C recorder, then perform offline evidence review
```

Terminal C 的 recorder 只启动多个 `ros2 topic echo` 订阅并写入带时间戳的 session
目录，不 publish、不调用 service、不操作 authority、E-STOP、CAN、GPIO、PWM、motor 或
fan：

```bash
python3 scripts/hardware_verification/record_gate_evidence.py \
  --output-root ~/windarmor_evidence
```

它默认连续记录 authority、Flight command、motor feedback、安全状态、ownership 和 fan
PWM；JSON `--config` 或任意重复的 `--topic NAME=/topic:package/msg/Type` 都会**替换**
默认 topic 集，而不是追加。需要额外 topic 时，本地配置必须同时完整列出所需默认项和
额外项。看到 `READY` 后再进入 gate trigger；结束时按 `Ctrl+C`，manifest 会记录每个
child 的退出码、样本状态和停止时间。

`ros2 topic echo ... --once` 只等待订阅建立后的下一条消息，可以用于稳态抽查，但不
适合作为 ACTIVE、handoff、ownership 或 command transition 的唯一历史证据。这些瞬态
证据必须优先使用 continuous recorder；optional `--once` 文件缺失或为空不能覆盖已由
continuous log 捕获的有效最终状态。

operator physical observation 始终是独立 required evidence。场景结束后默认由人工离线
审查 trigger 记录、continuous ROS history、物理观察和退出后安全状态；只有人工判定容易
出错、条件复杂且重复执行价值明显时，才考虑新增 gate-specific analyzer。analyzer 不能
替代 physical observation，也不能单独宣布 hardware PASS；B2 已有 analyzer 不意味着
C1/C2/C3/C4 必须分别新增 analyzer。

`record_gate_evidence.py` 是长期通用 verification infrastructure。
`analyze_b2_evidence.py` 及其测试属于 v0.4.0 verification-cycle scoped tooling，在 Gate C、
Gate D 和本次 release 收口完成前暂时保留；v0.4.0 正式形成 tag/release 后，再由独立
cleanup task 判断是否从未来 master 删除。历史可追溯性由 Git tag/history 保留，不要求
旧 Gate 专用 analyzer 永久留在未来 master。

### 2.4 Global immediate-stop conditions

- logical motor、CAN ID、实际轴或方向不一致；
- 非批准轴运动、目标跳向零位、超出批准 offset 或接近机械干涉；
- fan 错侧、异常方向、非批准旋转或超过 Flight PWM 上限；
- fault bit、临界温度、CAN/serial 异常、owner/token/sequence 不一致；
- unexpected ACTIVE、旧 epoch/generation/target 重现或 legacy 自动恢复；
- E-STOP、watchdog、command lease 或 safe-stop 没有 fail-closed；
- 异常声音、振动、气味、温升、结构松动或日志丢失；
- 操作人或现场安全观察员要求停止。

立即发布系统急停（若 ROS 仍可用）：

```bash
ros2 topic pub --once /e_stop std_msgs/msg/Bool "{data: true}"
```

同时准备直接切断对应 actuator 动力。不得把 ROS 命令作为物理断电的替代。

## 3. Gate A — Physical + Powered Read-only

当前执行记录：A0、A1 已由用户完成并报告 PASS。Task 6.2.1 修复 Jazzy launch
兼容性后，用户另行执行 passive zero-TX observation：transport connected，但四台
CyberGear 均持续 `has_feedback=false`。因此 passive zero-TX feedback 记为
`NOT SUPPORTED BY OBSERVED HARDWARE BEHAVIOR / NOT A RELEASE REQUIREMENT`，不再要求
retry，也不把缺失 frame 伪造成硬件 PASS。

### A0 — Power-off physical inspection

【本阶段需要通电】

- 无。

【本阶段必须保持断电】

- CyberGear motor bus；
- fan/ESC；
- 检查接线时 Raspberry Pi/逻辑电源也断开。

【上电前检查】

1. 确认全部电源确实断开并完成放电；机器人固定，机械范围内无人。
2. 核对 CAN H/L/GND、终端电阻、CAN ID 4/3/2/1 与四个 logical motor。
3. 核对 IMU X+ 向前、Y+ 向左、Z+ 向上。
4. 核对 GPIO12/pin32 为左 fan、GPIO26/pin37 为右 fan、GND 和 ESC 极性；确认旧
   GPIO13/pin33 没有连接右 ESC PWM。
5. 手动缓慢确认机械净空、线缆余量、软限位与机构范围，并确认 motor/fan 可独立断电。

【Terminal 1】

执行：

```bash
cd ~/workspace/WindArmor
git rev-parse HEAD
git status --short --branch
sed -n '1,360p' src/imu_cybergear_ros2/config/imu_cybergear_params.yaml
sed -n '1,220p' src/windarmor_fan_controller/config/fan_params.yaml
sed -n '1,220p' src/windarmor_flight_control/config/flight_control.yaml
```

预期：只读出当前基线；无 ROS node、CAN、串口或 GPIO 访问。

【物理预期】

应该发生：只有断电目视、标签和缓慢手动净空检查。

绝对不应该发生：上电、保持力、PWM/ESC 声音、电机或 fan 主动运动。

【PASS】所有映射、接线、极性、机械净空和物理断电手段都有证据；无法在断电下
确认的动态方向记 `NOT VERIFIED`，不伪造 PASS。

【FAIL / 立即停止】任何接线身份、极性、GND、机械范围或安全断电方式不明确。

【安全退出】保持所有动力断开，保存照片和检查记录；FAIL 项修复前不进入 A1/A2。

### A1 — Sensor / IMU observation

【本阶段需要通电】

- Raspberry Pi 与 IMU 所需逻辑电源。

【本阶段必须保持断电】

- CyberGear motor bus；
- fan/ESC。

【上电前检查】完成 A0、software gate green、IMU 固定牢靠且姿态动作可由人工小幅完成。

【Terminal 1】

```bash
cd ~/workspace/WindArmor
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch imu_cybergear_ros2 imu_cybergear_system.launch.py \
  start_controller:=false start_rviz:=false
```

预期：只启动 IMU driver；明确会访问 `/dev/imu_usb`，不创建 motor controller。

【Terminal 2】

```bash
source /opt/ros/jazzy/setup.bash
source ~/workspace/WindArmor/install/setup.bash
ros2 topic echo /imu/data_raw sensor_msgs/msg/Imu
```

【Terminal 3】

```bash
source /opt/ros/jazzy/setup.bash
source ~/workspace/WindArmor/install/setup.bash
ros2 topic echo /imu/status std_msgs/msg/String
```

预期 ROS 输出：`/imu/data_raw` 连续合法 quaternion/velocity/acceleration，status 明确
connected；物理小幅 roll/pitch 与坐标定义一致。

【物理预期】应该发生：手动改变机体姿态时读数相应变化。绝对不应该发生：任何
motor/fan 动作、CAN 初始化或 GPIO/PWM 输出。

【PASS】连接与数据新鲜，轴和符号与物理方向一致。【FAIL / 立即停止】无数据、无效
quaternion、轴/符号错误、出现 actuator node 或动作。

【安全退出】Terminal 1 `Ctrl+C`，确认 IMU node 退出，再断逻辑电源；motor/fan 继续断电。

### A2 — Motor passive observation（历史结果；非 release requirement）

【本阶段需要通电】

- Raspberry Pi、IMU/CAN HAT 逻辑电源；
- CyberGear motor bus。此子阶段明确需要给 CyberGear motor bus 通电。

【本阶段必须保持断电】

- fan/ESC。

【上电前检查】A0/A1 PASS；机器人固定；所有 motor 有机械净空；physical motor kill
可立即操作；确认 graph 中无普通 motor controller、fan controller 或旧 Runtime。

【Terminal 1】

```bash
cd ~/workspace/WindArmor
sudo ./scripts/setup_can.sh can10
ip -details link show can10
```

预期：`can10` 为 UP、1 Mbps。此系统配置命令只能在本阶段获授权后执行。

【Terminal 2】

```bash
cd ~/workspace/WindArmor
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch windarmor_bringup windarmor_observation_only.launch.py
```

预期：启动 IMU RX、relative observer、motor passive-RX observer 和 takeover 固定为
false 的 Flight Runtime；不初始化 CyberGear actuator，不启动 fan/GPIO。

【Terminal 3】

逐条执行并分别记录：

```bash
ros2 topic echo /motors/observer_status std_msgs/msg/String
ros2 topic echo /motors/feedback windarmor_interfaces/msg/MotorFeedbackArray
ros2 topic echo /imu/relative_roll_pitch geometry_msgs/msg/Vector3Stamped
ros2 topic type /flight_control/dry_run/status
ros2 topic echo /flight_control/dry_run/status
ros2 topic echo /flight_control/authority/status windarmor_interfaces/msg/FlightAuthorityStatus
ros2 node list
ros2 topic list
ros2 service list
```

已观测 ROS 结果：transport connected；四 logical motor 都出现，但均持续
`has_feedback=false/valid=false/fresh=false`。这确认 passive zero-TX 不能作为 Flight
feedback 来源；observer 没有 authoritative control safety latch，fan state 保持 unknown，
也不因此扩展 observer architecture。

【物理预期】应该发生：motor 始终静止。绝对不应该发生：enable、set-zero、目标写入、
保持位置变化、fan PWM 或旋转。

【历史结论】零 TX 无 passive feedback：`NOT SUPPORTED / NOT REQUIRED`。任何 movement、
actuator initialization、unexpected command publisher 或映射错误仍是 FAIL/立即停止。

【安全退出】Terminal 2 `Ctrl+C`；确认 observer 关闭 reader 后先断 motor bus，再按现场
顺序断逻辑电源；fan/ESC 全程保持断电。

### A3 — Fan read-only boundary

当前没有真实 fan RPM/thrust/ESC feedback。Gate A 中 fan/ESC 保持断电，不启动
`fan_controller`，不把缺失状态伪装成 stop measurement。A3 PASS 只表示该 unknown
边界被正确保留，不表示 fan hardware 已验证。

## 4. Gate B — Bounded Actuator Verification

Gate B normal-controller feedback baseline 已确认：四台 CyberGear initialize PASS、
`MANUAL_RUNNING` PASS，持续 feedback 均为 `has_feedback=true`、`valid=true`、
`fresh=true`、`healthy=true`、`fault_flags=0`。Flight DRY_RUN 也已 PASS。

该 baseline 同时暴露了 cold startup 固定写 `loc_ref=0.0` 会使掉电后已失效的内部零位
产生主动运动风险。Task 6.2.3 已完成 software-only 修复：configure 先从本次
`run_mode`/`limit_spd` 的 type-18 -> type-2 response 取得可信实测位置，再以同一原生
坐标作为首次目标；无可信反馈则 fail-closed，不自动 set-zero。当前状态是：

```text
Gate A0: PASS
Gate A1: PASS
Task 6.2.2: SOFTWARE + HARDWARE PASS
Gate B feedback baseline: PASS
Gate B Flight DRY_RUN: PASS
B0 cold-start hold-current: PASS
Fan passive startup fix: SOFTWARE PASS
Fan E-STOP preservation: SOFTWARE PASS
Flight fan state contract: SOFTWARE PASS
Task 6.2.7 shutdown/watchdog tooling: SOFTWARE PASS
B1 bounded takeover functional result: HARDWARE PASS
B1 procedural ACTIVE <=3 sec: NOT MET / TO BE CLOSED DURING GATE C / C4B
B2 bounded fan verification: HARDWARE PASS
Gate B: COMPLETE
Gate C / C1: HARDWARE PASS
Gate C / C2: HARDWARE PASS
Gate C / C3: HARDWARE PASS
Gate C / C4: NOT AUTHORIZED / NOT EXECUTED
Gate C: IN PROGRESS / NOT COMPLETE
```

B1 attempt #1 在调用 prepare 前停止。motor safety 为 PASS，Flight DRY_RUN 为 PASS；
fan safety 当时真实观察到 `enabled_observed=true`、`enabled=true`，但 manager 因首条
`/fans/enabled` 晚于首个 control tick 而锁在 `DISABLED`，
`passive_for_takeover=false`。本次 attempt 没有取得 Flight authority，没有发送 Flight
motor command，也没有 fan actuation。Task 6.2.4 已通过纯软件修复区分“首条 enabled
观测尚未到达”与“已观测后的 false/stale/invalid”；运行期 `DISABLED`、E-STOP、owner
lease 和 Flight preflight 均保持 fail-closed。

B1 retry attempt #2 已接受 prepare，随后通过 `/e_stop=true` 终止；没有保存到可确认
`FLIGHT_CONTROL ACTIVE` 的证据，也没有保存到 `left_pitch +0.05 rad` 有界运动证据，因此
结果仍为 inconclusive，不标为 FAIL 或 PASS。该轮观察到 Runtime 重复拒绝 fan safety：

```text
fan e-stop latch conflicts with control state
```

Runtime 重启后正确因 `global_estop_active` 保持 inhibited。Task 6.2.5 已用 pure core、
fake owner endpoint、mock ROS 与 in-memory Runtime 回归修复 fan E-STOP dominance：锁存后
rollback/revoke/safe-stop/lease cleanup 仍清除 owner、token 和旧 command，但 control
state 保持 `EMERGENCY_STOP`，只有显式 reset 可离开。该结果是 SOFTWARE PASS；在当时
B1 仍仅为 `READY FOR RETRY`，不是 hardware PASS。

B1 attempt #3 已接受 prepare，达到 `preflight_ready=true` 并启动 combined ownership
handoff；fan core 正确进入正式 `FLIGHT_WAITING`，但 Flight safety adapter 的旧 allowlist
遗漏该状态，Runtime 报告 `unknown fan control state` 后 fail-closed 进入 INHIBITED。
该轮没有确认 `authority_state=ACTIVE`、`command_authority=FLIGHT_CONTROL`、
`actuation_allowed=true` 或 `left_pitch +0.05 rad` 运动，因此仍为
`INCONCLUSIVE / FAIL-CLOSED BEFORE ACTIVE`，不是 hardware PASS。Task 6.2.6 已用实际 fan
core snapshot、strict adapter 和 fake owner endpoints 走通 reserve/commit/readback/cutoff/
atomic ACTIVE 软件路径，并保持 E-STOP、passive、enabled、legacy owner、unknown state、
epoch/generation 和 lease 检查。该结果是 SOFTWARE PASS；在第三次 attempt 结束时 B1
仍仅为 READY FOR RETRY。

B1 closure run 随后取得了完整 functional evidence：prepare、ARMING、
READY_TO_TAKEOVER、atomic ACTIVE、`command_authority=FLIGHT_CONTROL`、两 owner committed
且 token 匹配、atomic cutoff 存在、`actuation_allowed=true`；`left_pitch` 达到
`baseline +0.05 rad`，其他三轴保持 baseline，全部相关 feedback valid/fresh/healthy、
`fault_flags=0`。E-STOP 到达 lower-level 后 motor 从 `AUTO_RUNNING` 进入
`EMERGENCY_STOP`，motor/fan latch 均为 true，fan state 为 `EMERGENCY_STOP`，Flight
INHIBITED/NONE、actuation disabled、两 owner 最终 NONE，且没有 shake 或异常声音。因此
B1 functional hardware verification 为 PASS，不再为重复确认这些功能而重跑 B1。

该 closure run 的 procedural `ACTIVE <=3.0 sec` 未满足：临时 watchdog 在 ACTIVE 后先
sleep 2.5 秒，再启动新的 `ros2 topic pub --once`，process startup/DDS discovery 延迟使
实际 `/e_stop` 送达晚于 3 秒；这不是 Flight Runtime 或 lower-level E-STOP 响应慢。
Task 6.2.7 提供预热 publisher watchdog，严格 timing 证据将在 Gate C E-STOP 子场景顺便
闭合。对应的 Gate C E-STOP 子场景仍需新的单独硬件授权。

Gate B 保持现有 motor+fan atomic ownership，只允许一个域产生非停止动作。
`bounded_verification_controller` 从当前 ACTIVE authority session 的 fresh、valid、
healthy `MotorState.position_rad` 捕获一次 baseline；selected motor 每 tick固定为
`baseline + approved offset`，其他 motor 固定 hold baseline，不逐 tick 累加。

CyberGear set-zero reference 在 motor power loss 后失效。normal motor controller
configure 现在只保持本次实测当前位置，不会自动把它定义为零；B0 通过后，operator
仍必须把机器人摆到正确 physical reference posture 并显式 set-zero。完成这一步前，
不得进入 B1 或其他依赖准确机械坐标的 MANUAL/AUTO/HOME/Flight 动作。

### B0 — Cold-start Hold-current Verification

【本阶段需要通电】Raspberry Pi/IMU/CAN、CyberGear motor bus。

【本阶段必须保持断电】fan/ESC。

【上电前检查】软件 CI green；机器人可靠固定；四轴有机械净空；physical motor kill
就绪；明确本次通过完整 motor power cycle 验证，而不是只重启 Raspberry Pi。该场景
必须单独获得用户授权，不能沿用以前 Gate B 授权。

【唯一目标】controller 启动后四台电机保持上电时的当前物理位置，不主动移动到零。
先记录启动前姿态和预期静止边界；不得同时启动 Flight Runtime 或 fan/ESC。

【Terminal 1】仅在本阶段授权包含 CAN 系统配置时执行：

```bash
cd ~/workspace/WindArmor
sudo ./scripts/setup_can.sh can10
ip -details link show can10
```

【Terminal 2】

```bash
cd ~/workspace/WindArmor
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch windarmor_bringup windarmor.launch.py \
  start_fans:=false enable_motor_keyboard:=false
```

【Terminal 3】分别观察：

```bash
ros2 topic echo /motors/feedback windarmor_interfaces/msg/MotorFeedbackArray
ros2 topic echo /motors/control_mode std_msgs/msg/String
ros2 topic echo /motor/status std_msgs/msg/String
```

预期 ROS 证据：ID4/3/2/1 均记录“启动保持已从实测位置初始化”，第一次 position
target 与该 ID 的 measured position 一致；随后 feedback probe 重发同一 hold target；
四轴 feedback 保持 valid/fresh/healthy 且无 fault。

预期物理行为：四台电机只建立当前位置保持，不发生向零位的主动运动。任一轴出现
可见位置跳变、方向错误、target/actual 不一致、fault、温升或异常声音，立即急停并
切断 motor power，结果为 FAIL。

保持验证 PASS 后，operator 确认机器人处于正确 physical reference posture，再显式
调用：

```bash
ros2 service call /motors/set_zero std_srvs/srv/Trigger "{}"
```

确认四轴 feedback 接近零后安全退出。本步骤只验证 cold-start
hold 和显式 zero 流程，不执行 bounded Flight takeover，也不要求重跑完整 Gate A 或
旧 Gate B feedback baseline。

【安全退出】发布 `/e_stop=true` 并确认停止，退出 normal controller，先断 motor bus，
再按现场顺序断逻辑电源；fan/ESC 全程断电。

### B1 — Motor bounded verification

B1 closure run 已按上方证据完成，functional hardware result 为 PASS。第一次至第三次
attempt 的 fail-closed 结果和相应 Task 6.2.4–6.2.6 软件修复继续保留为历史证据，但不再
把 B1 标为 READY FOR RETRY。不得仅为重新确认 atomic ACTIVE、`left_pitch +0.05 rad`、
其他三轴 hold、motor/fan E-STOP 或 owner release 而重跑 B1；严格 `<3 sec` timing 改由
Gate C 的预热 watchdog 获取。

以下 B1 命令仅保留已完成流程的审计记录，不构成新的执行授权。

【本阶段需要通电】Raspberry Pi/IMU/CAN、CyberGear motor bus；fan manager/controller
需要逻辑运行以满足 combined ownership。

【本阶段必须保持断电】fan/ESC 动力；若电气结构不能独立断开 fan/ESC，STOP 并重新
评审，不得假设可安全执行。

【上电前检查】Gate A 和 B0 PASS，且本次 motor power cycle 后已在正确 reference
posture 显式 set-zero；唯一 logical motor、方向、`<APPROVED_OFFSET_RAD>`、初始
位置、软限位余量、持续时间和 physical kill 已由用户确认；robot 固定；所有占位符
替换；offset 必须非零、有限且不会越过软限位。首次只允许一个 axis。

【Terminal 1】

```bash
cd ~/workspace/WindArmor
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch windarmor_bringup windarmor.launch.py \
  enable_motor_keyboard:=false
```

预期：normal motor/fan safety owners 启动；fan GPIO 输出 stop，但 ESC 动力断开；电机
完成既有初始化并停在确认的零位。任何意外初始化运动立即停止。

【Terminal 2】

仅在替换占位符并再次获准后执行：

```bash
cd ~/workspace/WindArmor
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 run windarmor_flight_control flight_control_runtime_node --ros-args \
  --params-file src/windarmor_flight_control/config/flight_control.yaml \
  -p controller_factory:=windarmor_flight_control.algorithms.bounded_verification_controller:create_controller \
  -p verification_controller_enabled:=true \
  -p test_motor_name:=<APPROVED_LOGICAL_MOTOR> \
  -p motor_test_offset_configured:=true \
  -p motor_test_offset_rad:=<APPROVED_OFFSET_RAD> \
  -p fan_left_test_command:=0.0 \
  -p fan_right_test_command:=0.0 \
  -p flight_takeover_enabled:=true
```

未替换的命令无效且禁止执行。Runtime 启动后不会自行 prepare。

【Terminal 3】

不要再靠肉眼追踪高速 `topic echo`。在调用 prepare 前，分别在独立终端启动持续文件
记录：

```bash
rm -f /tmp/windarmor_b1_authority.log /tmp/windarmor_b1_feedback.log
stdbuf -oL ros2 topic echo \
  /flight_control/authority/status \
  windarmor_interfaces/msg/FlightAuthorityStatus \
  > /tmp/windarmor_b1_authority.log
```

```bash
stdbuf -oL ros2 topic echo \
  /motors/feedback \
  windarmor_interfaces/msg/MotorFeedbackArray \
  > /tmp/windarmor_b1_feedback.log
```

另开终端持续显示文件中明确的 ACTIVE transition，不直接显示全部高速消息：

```bash
tail -n 0 -F /tmp/windarmor_b1_authority.log \
  | stdbuf -oL grep --line-buffered 'authority_state: ACTIVE'
```

closure run 使用过下列临时命令；它会在 sleep 后才启动新 ROS process，已确认不适合
严格 timing measurement，不得在 Gate C 复用：

```bash
read -r -p '确认已看到 ACTIVE 后按 Enter；3 秒内将自动 E-STOP' \
  && sleep 3 \
  && ros2 topic pub --once /e_stop std_msgs/msg/Bool "{data: true}"
```

全部记录进程和急停终端就绪，且前述状态 fresh/healthy、安全观察员就位后，才在另一
终端执行 prepare：

```bash
ros2 topic echo /motors/ownership_state windarmor_interfaces/msg/OwnershipState
ros2 topic echo /fans/ownership_state windarmor_interfaces/msg/OwnershipState
ros2 service call /flight_control/authority/prepare std_srvs/srv/Trigger "{}"
```

该命令没有满足 procedural `ACTIVE <=3.0 sec`，但不推翻已经保存的 B1 functional
evidence。Gate C E-STOP 子场景必须改用下方预创建 publisher 的 watchdog；如果 10 秒内
没有 ACTIVE，watchdog 会 E-STOP、记录 `NO ACTIVE`，且不会自动重复 prepare。

预期 ROS 输出：两 owner 经 reserve/commit 后使用同一 epoch/generation；Runtime
atomic ACTIVE；首个 command state sequence 大于 cutoff；完整四 motor frame；selected
target=`captured baseline + offset`，其他 target=`captured baseline`；fan=`0.0/0.0`。

【物理预期】应该发生：只有批准 axis 按批准方向作 bounded movement，其他 motor
保持，fan 静止。绝对不应该发生：第二轴运动、逐 tick 漂移、目标跳到零、fan 起转。

【PASS】轴、方向、bounded target、完整 frame、hold、stop fan、revoke 均有证据。
【FAIL / 立即停止】任何未批准动作、drift、fault/温升、owner/sequence 错误。

【安全退出】先发布 `/e_stop=true`；确认 motor halt、fan stop、Runtime INHIBITED 和
owner 最终 NONE；Terminal 2/1 分别 `Ctrl+C`；先断 motor bus，再断逻辑电源。不得
自动 reclaim legacy。

### B2 — Fan bounded verification

B2 已由用户在一次独立授权的 Raspberry Pi 5 + WindArmor 实机 session 中完成，正式结果：

```text
B2 bounded fan hardware verification: HARDWARE PASS
Gate B: COMPLETE
```

最终配置为 LEFT GPIO12/pin32、RIGHT GPIO26/pin37；只给 LEFT fan/ESC 动力，RIGHT ESC
独立断电。Flight command 为 LEFT `0.05`、RIGHT `0.0`，对应预期 PWM 为 1210/800 us；
`test_motor_name=left_lift`、明确配置 motor offset `0.0 rad`，四台 motor 只保持各自 captured
baseline，不应发生 intentional motor movement。

用户报告的物理证据为：LEFT bounded response PASS、无意外 motor movement、无异常振动/
声音/气味、E-STOP 后 LEFT stopped PASS。continuous software evidence 同时捕获：

```text
ACTIVE complete evidence: PASS
Flight command 0.05 / 0.0 and four logical motor names: PASS
complete healthy/fresh/valid/fault-free 4-motor snapshot: PASS
fan PWM first: (800,800)
fan PWM left_max: 1210
fan PWM right_unique: [800]
fan PWM last: (800,800)
post-E-STOP authority, owner NONE and motor/fan latches: PASS
```

`post_fan_pwm.txt` 存在但为空，分类为 supplemental one-shot evidence capture failure，
不是 Runtime 或硬件 failure。它不影响 B2：同一次 continuous `fan_pwm.log` 已捕获最终
`(800,800)`，用户物理观察到 LEFT 在 E-STOP 后停止，authority、ownership 和两路 latch
也全部通过。RIGHT software command/PWM 全程保持 `0.0 / 800 us`，但 RIGHT ESC 在该次
最终 B2 测试中独立断电，因此不得表述为 RIGHT powered physical Flight verification。

以下流程保留为已完成 B2 的审计边界和未来同类 gate 的模板，不构成 B2 重跑授权。

【本阶段需要通电】Raspberry Pi/IMU/CAN、CyberGear motor bus、指定 fan/ESC 动力。

【本阶段必须保持断电】未测试 fan 通道如电气结构允许独立断电；否则明确记录两路
均上电，但未测试通道 command 必须为 `0.0`。

【上电前检查】B1 PASS；fan guard、GPIO12/26 和左右映射已确认；批准唯一 fan channel、
`<APPROVED_FAN_*_COMMAND>`、PWM 上限和持续时间；motor baseline/soft-limit margin
健康；不沿用 B1 授权。

【Terminal 1】与 B1 相同启动 normal system；先保持 fan/ESC 断电，确认 stop PWM 后再
按 ESC 既有安全顺序给本阶段批准的 fan/ESC 通电。

【Terminal 2】与 B1 相同，但 motor offset 必须由用户明确设为当次批准值；若本次只
允许 hold，可将明确批准的 offset 设为 `0.0`，仍必须保留
`motor_test_offset_configured:=true`。fan 参数只能一侧非零：

```bash
ros2 run windarmor_flight_control flight_control_runtime_node --ros-args \
  --params-file src/windarmor_flight_control/config/flight_control.yaml \
  -p controller_factory:=windarmor_flight_control.algorithms.bounded_verification_controller:create_controller \
  -p verification_controller_enabled:=true \
  -p test_motor_name:=<APPROVED_LOGICAL_MOTOR> \
  -p motor_test_offset_configured:=true \
  -p motor_test_offset_rad:=0.0 \
  -p fan_left_test_command:=<APPROVED_FAN_LEFT_COMMAND> \
  -p fan_right_test_command:=<APPROVED_FAN_RIGHT_COMMAND> \
  -p flight_takeover_enabled:=true
```

【Terminal 3：continuous evidence】

```bash
python3 scripts/hardware_verification/record_gate_evidence.py \
  --output-root ~/windarmor_evidence
```

看到 `READY` 后，gate-specific trigger/prepare 留在独立 Terminal 4。结束时按 `Ctrl+C`
停止 recorder，再离线分析 session：

```bash
python3 scripts/hardware_verification/analyze_b2_evidence.py <SESSION_DIR>
```

analyzer 只判定 B2 software evidence；exit code 0 表示 required software evidence PASS，
不能替代 operator 的 fan movement、motor movement、振动、声音或气味观察，也不能单独
宣布 hardware gate PASS。若保留额外 snapshot，可显式传入：

```bash
python3 scripts/hardware_verification/analyze_b2_evidence.py \
  <SESSION_DIR> --post-fan-pwm <POST_FAN_PWM_FILE>
```

`post_fan_pwm` 缺失或为空只产生 supplemental warning；continuous final PWM 仍是 required
evidence。若 supplemental 文件存在且包含与 stop 冲突的有效 PWM，则 analyzer fail-closed。

已完成 B2 的 prepare 命令属于当次已结束的授权记录：

```bash
ros2 service call /flight_control/authority/prepare std_srvs/srv/Trigger "{}"
```

预期 ROS 输出：combined owner ACTIVE；motor complete frame 等于 captured baseline；
批准 fan normalized command 经既有 mapping/ramp 输出且不超过 1400 μs；另一侧 stop。

【物理预期】应该发生：仅批准 fan 有 bounded 起转/响应，motor 不动。绝对不应该
发生：motor movement、错侧 fan、直接跳 1.0、把 normalized value解释成 thrust。

【PASS/FAIL/立即停止】正确侧、bounded ramp、stop/revoke 全部符合为 PASS；错侧、异常
振动/声音/气味、motor movement、PWM/owner 异常立即 FAIL 并停止。

【安全退出】`/e_stop=true`，确认 `[800,800]` 和 motor halt；先断 fan/ESC，再退出
Runtime/system，再断 motor bus和逻辑电源。

## 5. Gate C — Fail-closed Verification

Gate C 只在 Gate B PASS 后逐项授权；一次只注入一个条件。初始 command 必须沿用
当次重新批准的 B1/B2 bounded 值，不自动沿用旧 session。每项预期均为 motor halt、
fan stop、owner NONE 和无 automatic legacy recovery。仅当 Runtime 本身仍可运行时才要求
它 fail-close 到 `INHIBITED`；C2 会冻结 exact Runtime process，不能要求冻结进程继续 tick、
rollback 或发布新的 authority state。

Task 6.2.7 已完成 software-only tooling。C1 在 2026-08-20 的最终 retry 中取得
`HARDWARE PASS`；C2 最终 session 也在同日取得 `HARDWARE PASS`。C3 的首次 powered
session 因 OLD Runtime 非零退出保留为 `NOT VERIFIED` 历史；production shutdown 修复和
software-only real-process smoke 完成后，2026-08-21 final powered retry 已取得
`HARDWARE PASS`。C4 仍未授权、未执行，因此 Gate C 仍为
`IN PROGRESS / NOT COMPLETE`。预热 E-STOP watchdog 只用于 C4b，不能在 C2/C3/C4a 中
干扰待验证的 lease、shutdown 或 owner-loss fault。后续真实执行仍需按子场景重新满足
十项授权门槛，C1/C2/C3 PASS 不自动授权 C4。

本计划记录已完成 C1/C2/C3 的批准边界，并定义其余子场景的证据需求，不授权新的 trigger、
prepare、lifecycle transition、E-STOP 或任何带电动作。C4 占位值不得从
B1/B2/C1/C2/C3 历史 session 自动视为已批准或自动复用：

| 子场景 | motor logical axis / offset | LEFT/RIGHT fan command | powered actuator combination | 最大 ACTIVE 持续时间 |
|---|---|---|---|---|
| C1 | `left_pitch / +0.05 rad` | `0.05 / 0.0` | motor bus 与 LEFT ESC powered；RIGHT ESC OFF | `10 s`；实测 ACTIVE 到 deactivate request `3.023377119 s` |
| C2 | `left_pitch / captured baseline +0.05 rad`；其余 3 轴 baseline hold | `0.05 / 0.0` | motor bus 与 LEFT ESC ON；RIGHT ESC OFF | `10 s`；实测 ACTIVE 到 SIGSTOP `3.004192545 s` |
| C3 | final powered retry：`left_pitch / captured ACTIVE-session baseline +0.05 rad`；其余 3 轴 baseline hold | `0.05 / 0.0` | motor bus 与 LEFT ESC ON；RIGHT ESC OFF | `10 s`；实测 legal ACTIVE 到 exact-PID SIGINT `3.005395510 s`；HARDWARE PASS |
| C4a | TO BE SET BEFORE EXECUTION | TO BE SET BEFORE EXECUTION | TO BE SET BEFORE EXECUTION | TO BE SET BEFORE EXECUTION |
| C4b | TO BE SET BEFORE EXECUTION | TO BE SET BEFORE EXECUTION | TO BE SET BEFORE EXECUTION | TO BE SET BEFORE EXECUTION |

每个子场景都必须把 trigger evidence、continuous ROS evidence、operator physical evidence
和 no-automatic-recovery evidence 一起人工离线审查；任一 required evidence 缺失均为
`NOT VERIFIED`，不能由另一类证据替代。C1/C2/C3 的最终分类均由人工离线审查完成，没有新增
Gate C analyzer。

### C1 — Runtime rollback from stale required input

【最终结果】`HARDWARE PASS`。最终 evidence session 为
`/home/h-goal/windarmor_evidence/gate-evidence-20260820T024541.024256Z-924522`，Git
基线为 `e327517ea025d609c8e0f6dabd27b3f031541239`。recorder manifest 为
`COMPLETED / SIGINT`，10 个 required topic 均有记录，所有 stderr 为空，
`abnormal_child=null`、`cleanup_timed_out=false`。

四类闭合证据如下：

1. **Trigger / IMU lifecycle：** helper 在 prepare 前完成 pre-arm；检测 ACTIVE 后
   `3.023377119 s` 请求 deactivate，低于批准的 `10 s` 最大 ACTIVE 持续时间；service
   success 为 true、helper result 为 PASS、退出码为 0。lifecycle history 只记录
   `active -> deactivating -> inactive`；366 条 IMU raw sample 的最后 stamp 为
   `1787193986.822868824`，之后没有新 sample。
2. **Runtime / command / ownership：** authority 在 `1787193983.818789482` 进入
   `ACTIVE / FLIGHT_CONTROL / generation 1 / actuation_allowed=true`，在
   `1787193986.968290806` 因 `required_inputs_stale` 进入
   `INHIBITED / NONE / generation 0 / actuation_allowed=false`。command sequence 仅为
   0–151；最后一帧 stamp `1787193986.926113605`，早于 stale，stale 后 command 数为 0，
   没有旧 target replay。motor owner 于 `1787193986.987437010`、fan owner 于
   `1787193986.978000402` 从 `FLIGHT_CONTROL` 回到 `NONE`。
3. **Bounded actuator：**全部 152 条 command 均保持 `left_pitch = baseline +0.05 rad`、
   其他三轴 baseline hold、LEFT/RIGHT fan command `0.05/0.0`。feedback 显示
   `left_pitch` 从约 `-0.00019 rad` 到约 `+0.05081 rad`；其他三轴仅有约
   `±0.001 rad` 量化范围。LEFT PWM 由 800 按 10 us 阶梯升至 1210 后回到 800，RIGHT
   全程为 800，最终为 `[800,800]`。motor safety 从 `AUTO_RUNNING` 回到
   `MANUAL_RUNNING`，fan safety 从 `FLIGHT_ACTIVE` 回到 `SAFE_STOP`。
4. **Physical / no recovery / safe exit：**operator 确认 ACTIVE 期间 LEFT fan 有低幅旋转，
   其余三台电机没有运动；`left_pitch` 幅度很小，肉眼不作为精确角度判据。没有错误轴、
   错侧 fan、异常大幅运动、机械干涉、异常振动/声音/气味/温升；stale 后 motor 与 LEFT
   fan 均停止。LEFT fan 在 motor 通电时已处于批准的 powered 边界，不影响该 PASS，
   RIGHT ESC 全程断电。故障后 recorder 继续记录约 235 s，authority ACTIVE sample、
   `actuation_allowed=true` sample 和新 command 均为 0，IMU 没有重新 active。最终确认
   LEFT ESC 与 motor 动力总线均已物理断电。

本 session 的 motor/fan safety history 全程为 `e_stop_latched=false`，且没有单独保存的
`/e_stop=true` 发布 transcript，因此不把 E-STOP 写成实际 safe-exit 证据。C1 PASS 的退出
证据是 stale rollback 已使执行器停止、无自动恢复，以及 operator 最终确认动力物理断开；
这不替代后续 C4 的 E-STOP 验证。

【attempt history】证据链保留如下，局部结果不向最终 PASS 倒灌：

- 2026-08-19 初次 attempt：`NOT VERIFIED`，原因见下方历史记录；
- 2026-08-20 约 1 s retry：session
  `gate-evidence-20260820T020508.332840Z-886665`，helper 成功 deactivate，但 LEFT PWM
  最高仅 1020，bounded response 证据不足，仍为 `NOT VERIFIED`；
- 2026-08-20 计划 3 s 的 helper-aborted session：
  `gate-evidence-20260820T023505.409661Z-915048`，未检测到 ACTIVE，helper 以
  `ACTIVE_WAIT_TIMEOUT / exit 4` 结束，LEFT/RIGHT PWM 均为 800，不构成 C1 attempt；
- 2026-08-20 最终 3 s retry：上述四类证据全部闭合，分类为 `HARDWARE PASS`。

以下执行契约保留为本次 closure 的历史依据，不构成再次执行 C1 或其他硬件测试的授权。

【历史 attempt】2026-08-19 的 C1 实机执行结果为 `NOT VERIFIED`，不是 C1 HARDWARE
PASS，不得据此进入 C2。证据 session 为
`~/windarmor_evidence/gate-evidence-20260819T085722.750971Z-709316`。主要原因：

1. ACTIVE exposure 约 `16.385 s`，超过批准的 `10 s`；
2. lifecycle deactivate 后 normal launch 自动执行 `inactive -> activating -> active`，
   与该次 no-recovery boundary 冲突；
3. 实际 LEFT ESC powered sequence 与批准步骤不完全一致；
4. `+0.05 rad left_pitch` 不适合作为必须由肉眼精确确认的 physical evidence。

局部正向证据包括：`required_inputs_stale` 后没有新 executable Flight command 或旧 target
replay；Runtime `ACTIVE -> INHIBITED`；motor/fan ownership 均
`FLIGHT_CONTROL -> NONE`；软件 feedback 支持 `left_pitch` 到达约 `+0.05 rad`；LEFT fan
有 bounded response；operator 报告 stale/E-STOP 后 motor/fan 停止。这些局部证据不改变
最终 `NOT VERIFIED` 分类。

【最终 retry 使用的硬件状态】prepare 前 motor bus 与 LEFT ESC 均已通电，RIGHT ESC
保持 OFF；“motor 先上电、稍后才给 LEFT ESC 上电”不作为 PASS 所需精确顺序。prepare
前确认 fan software state 为 `SAFE_STOP`、PWM 为 `[800,800]`、LEFT 没有非批准旋转、
四 motor healthy/hold，且 physical kill ready。该记录不授权再次供电或执行。

【Terminal 1】最终 C1 candidate 使用 manual IMU lifecycle mode：

```bash
ros2 launch windarmor_bringup windarmor.launch.py \
  enable_motor_keyboard:=false \
  imu_auto_activate:=false
```

该模式仍自动 configure IMU，但停在 inactive。系统和 recorder/helper 全部 ready 后，
operator 再显式执行一次：

```bash
ros2 lifecycle set /imu_driver_node activate
```

确认 IMU active、新 sample fresh 和全部 preflight 条件后才可 prepare。默认 production
launch 的 `imu_auto_activate:=true` 行为保持不变。

【Terminal 2/3/4】Runtime 使用当次重新批准的 bounded 值；continuous recorder 使用
完整 10-topic 本地 config。C1 trigger helper 必须在 prepare 前预创建 authority subscriber
和 `/imu_driver_node/change_state` client，看到 `C1_TRIGGER_READY` 后才允许 operator
prepare。最大 ACTIVE duration 为 `10 s`；约 1 s 的 retry 因 LEFT PWM 仅到 1020 而证据
不足，最终获批 retry 改为检测合法 ACTIVE 后约 `3.0 s` 自动请求 DEACTIVATE；两者都没有
在 ACTIVE 后新启动 `ros2 lifecycle set` 进程。

【Trigger evidence】最终授权执行使 IMU lifecycle 正常 deactivate，从而停止新 IMU
sample；保存了 pre-armed helper 的完整 stdout/stderr 和退出码。helper 至少记录
ACTIVE detected monotonic time、deactivate request monotonic time、两者间隔、service result
和最终 PASS/FAIL。等价的 lifecycle transition 为：

```bash
ros2 lifecycle set /imu_driver_node deactivate
```

仅有 CLI 成功还不够；证据必须同时显示 `/imu_driver_node/transition_event` 进入 inactive，
并显示 `/imu/data_raw` 的新 sample 确实停止。

【Continuous ROS evidence】默认 recorder 足以记录 Flight command、authority、两路 owner、
motor feedback/safety 和 fan PWM/safety，但不足以证明 IMU trigger。C1 使用一个不提交仓库
的本地 JSON config，完整重列 recorder 默认八项，并额外加入：

```text
imu_raw=/imu/data_raw:sensor_msgs/msg/Imu
imu_lifecycle=/imu_driver_node/transition_event:lifecycle_msgs/msg/TransitionEvent
```

`--config` 和 `--topic` 都会替换默认集，不能只提供上述两个额外 topic。required history
必须显示：ACTIVE 前存在合法 bounded Flight command；IMU sample 停止并使 required input
从 fresh 变为 stale；ACTIVE safety gate 报告 `required_inputs_stale`；该 tick 在 controller
update 前关闭 executable command dispatch，stale 后不再生成 command envelope，也不 replay
旧 target；Runtime 从 ACTIVE 进入 INHIBITED；motor/fan owner 从 FLIGHT_CONTROL 进入 NONE；
actuator software state 回到安全状态。`flight_imu_freshness_sec=0.2` 是 freshness threshold，
C1 不把当前多进程 recorder 扩展为精确证明 `0.200 s ± N ms` 的新 hardware timing SLA。

C1 验证的是 Runtime safety gate 在 controller update 前直接 rollback，不是 controller 主动
返回 `FlightCommand.safe_stop()` 的另一条安全路径。因此 `request_safe_stop=true` executable
frame 不是 C1 PASS 的必需证据，也不应期待 stale-input rollback 额外发布该 frame。owner
revoke 是本地 command cutoff、handoff/command tracking invalidation、authority inhibit 和
envelope sequencer invalidation 完成后的 best-effort cleanup；lower-level command lease/
backstop 在 revoke 异常时仍负责 fail-closed。

【Operator physical evidence】operator 不再需要肉眼精确确认 `left_pitch == +0.05 rad`；
selected target/feedback、其他三轴 hold 和 fan command/PWM 由软件证据负责。operator 必须
记录是否出现错误轴、明显异常大幅运动、机械干涉、错侧 fan、异常振动/声音/气味/温升，
LEFT fan 是否 bounded response，以及 stale 后 motor 与 LEFT fan 是否停止。软件日志不得
替代这些 physical safety observations。

【No automatic recovery evidence】C1 retry 使用 `imu_auto_activate:=false`；fault deactivate
后 IMU 必须保持 inactive，不得由 launch 自动恢复。本场景不为补证据而重新 activate IMU，
并须证明没有恢复旧 epoch/generation/token/target、legacy owner 或 actuator 动作。

预期因果关系：IMU sample cessation 使 required input stale；ACTIVE safety gate 检出
`required_inputs_stale`，Runtime 在调用 controller 前关闭 executable dispatch、使旧 command
tracking 和 envelope generation 失效、进入 INHIBITED，再 best-effort 撤销 combined owner；
旧 target 不被重发，lower-level lease/backstop 保持 fail-closed，最终 motor halt、fan stop。
绝对不应在 IMU 恢复后自动 ACTIVE。

PASS：上述四类证据都闭合。FAIL/立即停止：任何 actuator 继续、旧 frame 重发或 authority
自动恢复。

当时规划的安全退出：发布 `/e_stop=true`，确认停止；不重新 activate IMU 来恢复 Flight；
退出 Runtime/system 并按当前场景先 fan、后 motor 的顺序断电。最终 session 的实际可核验
退出证据以上述 stale stop、无自动恢复和物理断电为准，不补写未留 transcript 的 E-STOP。

### C2 — Command timeout / Runtime unresponsive

【最终状态】`HARDWARE PASS`。最终 session：

```text
/home/h-goal/windarmor_evidence/
  gate-evidence-20260820T070959.631365Z-1037382
```

本次已批准边界为 normal default IMU auto activation；motor bus 与 LEFT ESC ON、RIGHT ESC
OFF；`left_pitch = captured ACTIVE-session baseline +0.05 rad`，其余三轴 baseline hold；
LEFT/RIGHT fan command `0.05/0.0`；legal ACTIVE 后约 `3.0 s` 注入 fault；最大 ACTIVE
`10 s`。这只是已完成 session 的历史边界，不授权重复 C2 或任何新的硬件操作。

【Final evidence closure】recorder manifest 为 `COMPLETED / SIGINT`，`abnormal_child=null`、
`cleanup_timed_out=false`，默认 8/8 topic 均为 `SAMPLES CAPTURED`，全部 stderr 为空。
authority 与 fan ownership 文件开头各有一次 DDS lost notice，但都发生在 recorder 起始
DRY_RUN/NONE history；关键 fault 边界完整，motor ownership observation sequence
`1153 -> 1154`、fan ownership `9407 -> 9408` 连续，因此未形成 C2 required evidence 缺口。

pre-armed helper 验证 exact Runtime PID `1036555`，在 prepare 前输出 READY/ARMED，检测 legal
ACTIVE 后 `3.004192545 s` 发送 SIGSTOP，并在 `0.006321543 s` 后确认 process state `T/t`。
helper 记录的最后 command receipt 到 motor owner NONE、fan owner NONE、fan stop 分别为
`0.258244544 s`、`0.291357200 s`、`0.292747309 s`，结果为 PASS、exit 0；这些 monotonic
delta 是辅助 trigger/observation evidence，不单独构成 HARDWARE PASS 或 production SLA。

continuous history 共记录 131 帧 executable command，sequence `0-130` 连续且 payload
唯一：四轴始终为 captured baseline / selected `left_pitch +0.05 rad`，LEFT/RIGHT fan 始终为
`0.05/0.0`，`request_safe_stop=false`。最后一帧后没有新 command、safe-stop frame 或 stale
replay。以最后 command ROS stamp 为共同基准，motor owner 在 `0.260380983 s`、fan owner
在 `0.293756485 s` 进入 NONE，均落在本次批准的 `0.25-0.40 s` review window；边界前最后
ownership age 分别为 `0.237991288 s` 和 `0.263708737 s`，与各自 50/20 Hz 检查周期一致。

authority history 最终仍为 `ACTIVE / FLIGHT_CONTROL / actuation_allowed=true`，最后 authority
sample 仅比最后 command 晚 `0.000360727 s`，此后两条 frozen Runtime publication stream
一起停止。这是 C2 的预期 freeze evidence，不是 anomaly 或 recorder failure。

motor ownership 完整经过 `MANUAL -> FLIGHT_RESERVED -> FLIGHT_CONTROL -> NONE`，timeout 后
公开状态回到 `MANUAL_RUNNING / MANUAL`，但 owner 此后只保持 NONE，没有 ordinary owner
recovery。软件 feedback 显示 selected `left_pitch` 从约 `-0.00019 rad` 到约
`+0.04890 rad`，其余三轴仅在约 `-0.00250` 到 `+0.00020 rad` 的反馈量化/保持范围内；这是
软件 target/feedback 证据，不表述为 operator 肉眼精确确认角度。

fan ownership 完整经过 `NONE -> FLIGHT_RESERVED -> FLIGHT_CONTROL -> NONE`，fan safety 明确
记录 `Flight command timeout；等待显式重新授权`。continuous PWM 为 LEFT
`800 -> 810 -> ... -> 1200 -> 1210 -> 800 us`，RIGHT 全程 `800 us`；回到停止值后只出现
`[800,800]`。

【Operator physical evidence】用户报告 ACTIVE 时 LEFT fan 有明确低幅旋转；SIGSTOP/lease
expiry 后 LEFT fan 与 motor 均停止；无错误轴、意外运动、异常声音、振动、气味或温升。
用户同时确认旧 Runtime 从未 SIGCONT/`fg` 恢复，最终 actuator power 已物理断开，之后才对
exact frozen Runtime 使用 SIGKILL。物理停止、断电与现场异常观察属于 operator-provided
evidence，不伪装成 ROS software evidence。

【Buffered preflight history】更早的
`gate-evidence-20260820T065345.667453Z-1018147` 中，helper 实际 READY/ARMED，但 stdout 经
`tee` 后 block-buffered，operator 未实时看到 marker，因此正确地没有 prepare。该 session
只有 DRY_RUN，`flight_command.log` 为 0 字节，helper 为 `ACTIVE_WAIT_TIMEOUT`，没有 ACTIVE、
SIGSTOP 或 C2 fault injection，分类为 `C2 PRE-FLIGHT ABORTED / HARDWARE ATTEMPT NOT
STARTED`，不是 C2 FAIL 或 NOT VERIFIED。以后经 `tee` 运行 timing helper 应使用
`python3 -u` 或显式 flush；这不需要 production code 修改。

【与 C1 不同的安全 contract】C1 fault 发生时 Runtime 本身仍在运行，因此 Runtime 可在
control tick 内 fail-close、rollback 并发布 `INHIBITED`。C2 则对 exact Runtime process 发送
`SIGSTOP`；Linux 会冻结该进程的用户态执行，所以它不能继续 control tick、rollback、调用
revoke 或发布新的 `INHIBITED` authority sample。C2 不要求、也不得把“冻结后没有新的
INHIBITED frame”判作 failure。正确因果链为：

```text
legal ACTIVE
-> exact Runtime process enters STOPPED
-> Runtime command envelope stream ceases
-> frozen Runtime authority publisher also ceases producing new samples
-> motor/fan lower-level leases independently expire
-> motor halt and fan PWM [800,800]
-> motor owner NONE and fan owner NONE
-> no automatic legacy owner or ordinary command/movement
```

motor 与 fan 的 active command lease 均为配置中的 `0.25 s`，只由各 lower-level owner 在
收到合法 Flight envelope 时刷新。motor 在 `0.02 s` motion tick 检查 timeout，随后
`release_to_none()`、`halt_motion()`、退出 Flight `AUTO_RUNNING` 并发布 ownership；fan
manager 在 `0.05 s` control tick 检查 timeout，随后 `force_safe_stop()`、立即发布停止 PWM
并保持 owner `NONE`。motor controller timeout 后会回到 `MANUAL_RUNNING`/公开 `MANUAL`
label，但该 label 本身不是 legacy/manual recovery 证据：ownership 必须保持 `NONE`，且不得
出现普通 command 或 movement。

【Terminal / exact PID strategy】最终 session 中，Terminal A 保持 normal hardware system；
Terminal B 在 prepare 前前台运行 local-only `run_c2_runtime.sh`；Terminal C 在 prepare 前
启动默认 continuous recorder；Terminal D 在 prepare 前启动 `c2_sigstop_on_active.py`。
wrapper 通过 `ros2 pkg prefix windarmor_flight_control` 定位安装后的
`lib/windarmor_flight_control/flight_control_runtime_node`，先把自身 PID 写入 pidfile，再用
`exec` 替换为真正 executable；PID 因此保持不变，pidfile 不会误指向 `ros2 run` CLI
wrapper。wrapper 不 daemonize、不 prepare，也不执行额外 hardware control。

【Pre-armed trigger evidence】不再以 ACTIVE 后人工切换 terminal 并按 `Ctrl+Z` 为默认
trigger。local-only helper 在 prepare 前读取并验证 pidfile、PID 当前存在且属于当前用户、
`/proc/<pid>/cmdline` 明确为 `flight_control_runtime_node`，并拒绝 stopped/zombie/dead 或
ambiguous PID。它必须先观察 non-ACTIVE，再输出 `C2_TRIGGER_READY` 和
`C2_TRIGGER_ARMED`。只有满足下列完整 legal ACTIVE 条件才开始约 `3.0 s` delay：

```text
authority_state == ACTIVE
command_authority == FLIGHT_CONTROL
actuation_allowed == true
motor_committed == true
fan_committed == true
owner_tokens_match == true
```

delay 到期后 helper 对 exact PID 发送一次 `SIGSTOP`，轮询 `/proc/<pid>/status` 并确认
state 为 `T/t`；它绝不发送 `SIGCONT`，也不 prepare、publish Flight command/E-STOP、直接
控制 actuator、kill 或 restart Runtime。SIGSTOP 后 helper 继续只读观察 command cessation、
motor/fan owner `NONE` 和 fan PWM `[800,800]`，输出稳定 monotonic marker 与辅助 timing。
PID mismatch、初始即 ACTIVE、arm 前 ACTIVE、Runtime 意外退出、STOP 未确认或 bounded
observation window 内未看到 lower-level fail-close 都使 helper FAIL。helper PASS 只证明
trigger 与所观察软件 transition，不等于 C2 HARDWARE PASS；任何异常由独立 E-STOP 与
physical-power procedure 处理。

【Timing acceptance review】不得把 `0.250000 +/- N ms` 创造成新的 hardware SLA。
按当前 period，deadline 是最后合法 command receipt monotonic `+0.25 s`，timeout 条件为
`now > deadline`；nominal core detection 上界约为 motor `0.25 + 0.02 = 0.27 s`、fan
`0.25 + 0.05 = 0.30 s`，另有 ROS/DDS scheduling 与 recorder observation latency。本次
执行前批准的 evidence-review window 是：以 continuous history 中最后一帧 command 的 ROS stamp 为
共同基准，两 owner `NONE` 与 fan `[800,800]` 应在 **不早于 0.25 s 且不晚于 0.40 s** 出现；
超过 `0.40 s` 先分类为 timing review/NOT VERIFIED，而不是擅自放宽。helper receipt monotonic
与 lower-level receipt 分属不同 DDS subscriber，存在 delivery skew；helper 打印的 delta 只作
辅助交叉核对，不单独执行 `>=0.25 s` 下界判定，若略小于 `0.25 s` 必须结合共同 ROS stamp、
command sequence、`OwnershipState` history 和 timeout 前的
`last_valid_flight_command_age_sec` 审查。该 `0.40 s` 是本次基于现有 50/20 Hz period 批准的
verification upper window，不是 production SLA；单一时间源不独立宣布 PASS。

【Continuous ROS evidence】默认 recorder 的八项 topics 已覆盖 authority、Flight command、
motor feedback/safety/ownership 与 fan PWM/safety/ownership，C2 没有创建额外 JSON config。
最终 history 已显示 SIGSTOP 前合法 ACTIVE 与递增 command sequence、最后 command
stamp/sequence、之后 command/authority stream 停止、两 owner 从 `FLIGHT_CONTROL` 到
`NONE`、motor halt、fan `[800,800]` 及无 legacy/ordinary output。authority 在 SIGSTOP 后
没有新 frame 是 expected process-freeze evidence，不是 recorder failure；ownership
transition 的 PASS 证据以 continuous recorder 为准。

下列人工 echo 若保留，只是 optional live observation，不是 required historical evidence：

```bash
ros2 topic echo /motors/ownership_state windarmor_interfaces/msg/OwnershipState
ros2 topic echo /fans/ownership_state windarmor_interfaces/msg/OwnershipState
```

optional echo 缺失、追不上瞬态或输出不完整，不能推翻同一次 continuous recorder 中有效
的历史；反之，optional echo 也不能补救 recorder 缺失。

【No automatic recovery evidence】旧 Runtime 在本次 session 始终保持 STOPPED，没有
`fg`/`SIGCONT` 恢复；recorder 在 Runtime publication cessation 后继续记录 lower-level
NONE/stop history。C2 没有执行 restart；新 process/epoch isolation 属于 C3，不合并进本场景。

最终判定：exact Runtime STOPPED、command/authority cessation、两路独立 lease timeout、
halt/stop、owner release、无 legacy owner/ordinary movement 和旧进程无恢复都有 trigger、
continuous ROS 与 operator physical evidence，C2 为 `HARDWARE PASS`。

【实际安全退出】用户报告 lower-level fail-close 与 physical stop 确认后，recorder 继续获取
evidence；随后先物理断开 LEFT ESC 与 motor actuator power，旧 Runtime 全程保持 STOPPED；
operator 确认 actuator power OFF 后，才对 exact frozen PID 使用 `SIGKILL` 终止旧 process，
没有 `SIGCONT`/`fg`。物理断电与 SIGKILL 顺序是 operator-provided evidence，ROS logs 不能
独立证明。对不可响应的 frozen process，
`SIGTERM` 只能保持 pending，不能在不恢复执行的前提下完成用户态 graceful cleanup；断开
actuator power 后使用不可捕获的 `SIGKILL` 是无需恢复旧 Runtime 的确定性终止方案。C3
另行验证 graceful shutdown/restart；C2 没有执行 restart。

### C3 — Graceful Runtime stop/restart and stale authority

【最终状态】`HARDWARE PASS`。final authoritative recorder session：

```text
/home/h-goal/windarmor_evidence/
  gate-evidence-20260821T062508.750044Z-1360504
```

manifest 为 `COMPLETED / SIGINT`，`abnormal_child=null`、`cleanup_timed_out=false`，默认
8/8 required topics 均为 `SAMPLES CAPTURED`。recorder child 的 SIGINT exit code 全部分类为
`EXPECTED RECORDER CLEANUP EXIT`；这是 recorder 自身的统一清理，不是 Runtime exit failure，
也不构成 continuous history 缺口。瞬态人工 `topic echo` 只作辅助观测，authoritative history
以 continuous recorder 为准。

【Final OLD Runtime evidence】OLD PID 为 `1360007`，authority identity 为
`authority_epoch=254107543912471 / authority_generation=1`。pre-armed helper 在 prepare 前
输出 READY/ARMED，记录：

```text
ACTIVE_DETECTED_MONOTONIC=254150.764401440
SIGINT_SENT_MONOTONIC=254153.769796950
ACTIVE_TO_SIGINT_SEC=3.005395510
OLD_RUNTIME_EXIT_CONFIRMED_MONOTONIC=254154.544579109
LAST_OLD_COMMAND_RECEIVED_MONOTONIC=254153.750120267
MOTOR_OWNER_NONE_MONOTONIC=254153.826780042
FAN_OWNER_NONE_MONOTONIC=254153.843605997
FAN_PWM_STOP_MONOTONIC=254153.912634769
C3_STOP_RESULT=PASS
C3_STOP_OBSERVER_EXIT_STATUS=0
OLD_RUNTIME_EXIT_STATUS=0
```

continuous recorder 中 149 条 executable `FlightCommandEnvelope` 全部属于 OLD
epoch/generation，sequence 为 `0–148`，payload 始终为 approved bounded candidate：
`left_pitch = captured baseline +0.05 rad`、其余三轴 baseline hold、LEFT/RIGHT fan
`0.05/0.0`、`request_safe_stop=false`。C3 不依赖 final safe-stop envelope，该 false 值不是
缺陷。exact-PID SIGINT 后 command stream 永久停止；motor/fan owner 均从
`FLIGHT_CONTROL` 回到 `NONE`，token 清零；fan PWM 为 LEFT
`800 -> 10 us ramp -> 1210 -> 800`、RIGHT 始终 800，最终 `[800,800]`。OLD Runtime
clean exit 0，且没有 traceback、context-invalid rosout、
`Unable to convert call argument` 或 unexpected `RuntimeError`。helper monotonic delta 是
trigger/observation evidence，不创建新的 production actuator SLA。

【Final NEW Runtime isolation】NEW PID 为 `1363429`，明确不同于 OLD PID；NEW
`authority_epoch=254214835192101`，明确不同于 OLD epoch。continuous recorder 在 NEW
阶段持续记录 `DRY_RUN/NONE`、`authority_generation=0`、`attempt_present=false`、
`actuation_allowed=false`、motor/fan committed false；两 owner 持续
`NONE / authority_present=false / epoch=0 / generation=0`，fan PWM 持续 `[800,800]`。
NEW 启动后没有属于 NEW epoch 的 executable command，`NEW_RUNTIME_EXIT_STATUS=0`。

【Operator physical evidence】OLD ACTIVE 时 LEFT fan 低幅旋转；OLD SIGINT 后 motor 与 LEFT
fan 均停止；没有错轴、错侧 fan、异常声音、振动、气味或温升。NEW startup/观察窗口内
motor 与 fan 完全无动作且没有异常；最终 LEFT ESC physically OFF，motor bus physically
OFF。精确 payload、identity 与 timestamp 由 software/ROS evidence 负责，物理停止与异常
观察由 operator 提供，二者不互相替代。

最终 PASS 由三层证据闭合：提交 `96a23a9` 的 Python-managed signal shutdown production
fix；真实 Jazzy rclpy executor 的 software-only SIGINT/SIGTERM exact-PID smoke（均 exit 0、
无 traceback/context-invalid/conversion RuntimeError）与 `97 passed` targeted regression；
以及本 final powered retry 的 continuous ROS 与 operator physical evidence。software-only
证据本身不冒充 hardware PASS。C3 已闭合，不需要为重复确认而重跑，也不授权 C4。

【Pre-flight history】更早的
`gate-evidence-20260821T014852.009218Z-1200735` 因 local observer 对
`/proc/<pid>/status` 与 `/proc/<pid>/stat` 的非原子 process-state sample 要求完全相等而
false-positive abort。该 session 未 prepare、未 ACTIVE、未发送 SIGINT，分类为
`C3 PRE-FLIGHT ABORTED / HARDWARE ATTEMPT NOT STARTED`。helper 已改为 UID 取自 `status`，
state/start time统一取自同一次 `stat` sample，并保持 exact argv/PID-reuse 与 stopped/dead
防护。该预检记录不是 C3 attempt，不改变下方首次 powered session 的历史 `NOT VERIFIED`
分类，也不替代 final powered retry 的 PASS evidence。

【首次 powered session 历史分类】session
`gate-evidence-20260821T021811.696548Z-1220232` 的正式结果保持 `C3 NOT VERIFIED`，不是
C3 FAIL。正向
证据包括：连续 legal ACTIVE 到 exact-PID SIGINT `3.010030905 s`；helper PASS；motor/fan
physical stop；两 owner fail-close；NEW PID 与 `authority_epoch` fresh；NEW 持续
`DRY_RUN/NONE`、owners NONE、PWM `[800,800]`；无 restart movement、无 physical anomaly，
最终 actuator power OFF。operator 在 OLD exit status 1 后仍启动 NEW Runtime，因此 NEW-half
证据保留为 supplemental positive history，但不能覆盖该 session 的 OLD graceful-exit blocker；
后续 final PASS 也不改写这次历史分类。

该历史 session 的唯一 blocker 是 OLD Runtime 非零退出。exact-PID SIGINT 后原 rclpy default
signal handler 先 invalidated context；虽然 `finally -> destroy_node()` 与
`runtime_shutdown` rollback 已实际
运行，executor subscription `take_message` 仍抛出
`RuntimeError: Unable to convert call argument '0' to Python object`，并出现
`Failed to publish log message to rosout: publisher's context is invalid`。actuator fail-close
本身不是 blocker；该历史 OLD Runtime 的 `OLD_RUNTIME_EXIT_STATUS=1`。

【实际 Runtime graceful-shutdown contract】installed executable entry point 是
`flight_control_runtime_node = windarmor_flight_control.runtime.node:main`。修复后 `main()` 以
`SignalHandlerOptions.NO` 初始化 rclpy，并由 Python handler 统一处理 SIGINT/SIGTERM。首次
signal 先把两种后续 shutdown signal 设为 ignore，再抛出 `KeyboardInterrupt`；cleanup 开始前
再次保持 ignore，避免第二个 signal 中断 rollback。ROS context 因此在 `destroy_node()` 与
`runtime_shutdown` rollback 期间仍有效；node destruction 完成后才调用 `rclpy.shutdown()`，
最后恢复进入 `main()` 前的 Python signal handlers。普通 executor/Runtime `RuntimeError` 不被
捕获或伪装成 clean signal exit，仍传播并产生 nonzero。final retry 的 old/new transcript、
traceback 和 `PIPESTATUS[0]` 仍是 authoritative evidence；pure tests 不能替代真实 exit 0。

当 Runtime 处于 ARMING/READY/ACTIVE 时，`destroy_node()` 走
`_rollback_handoff("runtime_shutdown")`：先关闭 `_command_dispatch_enabled`，使 handoff、
owner commit tracking、last-command tracking 和 `CommandEnvelopeSequencer` 失效，并在内存中
把 authority 锁为 `INHIBITED/NONE`；随后对 motor/fan 各发起一次 asynchronous、nonblocking
best-effort revoke。shutdown 不等待 revoke response，不发布显式 safe-stop envelope，也不
显式发布最终 authority status。故 continuous recorder 可能保留 OLD Runtime 的最后一条
TRANSIENT_LOCAL ACTIVE authority 样本；这不是可期待的 final cleanup publication。

若 revoke service unavailable、调用/响应失败或进程在 response 前退出，motor/fan 各自的
`0.25 s` ACTIVE command lease 仍是 independent fail-close backstop。第一条合法 normal
envelope 才建立该 lease，之后只有 token/payload 合法且 command sequence 递增的 normal
envelope 才刷新；invalid、duplicate、wrong token 与 safe-stop 都不刷新。C3 不创造新的
毫秒级 actuator SLA。final review 分开记录：显式 revoke 是否实际发生，以及 revoke
未闭合时已由 C2 实机验证的 lease 是否使 motor/fan fail-close。

【实际 identity / sequence 字段】Runtime process-session 字段是
`FlightAuthorityStatus.authority_epoch`，构造时由 `time.monotonic_ns()` 产生一次；restart 必须
得到不同的正值。正式 owner token 不是独立字符串，而是 authority status 的
`(authority_epoch, authority_generation)`、command/ownership 的
`(authority_epoch, generation)`。同一 Runtime 的 prepare generation 从 1 递增；cancel/
inhibit 使当前 attempt 失效。无 prepare/authority 时 `authority_generation=0`、
`attempt_present=false` 且 `attempt_generation=0` 只是占位。每次 token 激活后
`FlightCommandEnvelope.command_sequence` 可合法从 0 开始并严格递增；新 token 下重新从 0
开始合法。`state_sequence` 也是 Runtime-local observation sequence，可在新 process 重新开始。
Motor/fan `OwnershipState.source_epoch` 标识 lower-level owner process，不是 Runtime identity；
C3 normal system 不重启时它可以保持不变。

【现有 stale/replay software evidence】pure tests 已覆盖 producer envelope 对 wrong/zero
epoch、wrong/zero generation、non-increasing command sequence 和 pre-cutoff state 的拒绝及
sequencer invalidation；authority core 覆盖 generation 不复用、旧/duplicate ack/commit 拒绝；
motor 与 fan command consumers 均覆盖 wrong epoch/generation、duplicate sequence 不刷新
lease、timeout/revoke 后旧 command 不恢复输出；Runtime handoff tests 覆盖旧 epoch readback
不能激活新 session、新 Runtime 必须使用 fresh authority epoch 完整 handoff，以及 owner
process epoch/sequence replay fail-close。motor 另有 old-epoch reserve 和 revoked generation
feedback-probe no-replay 测试。现有 fan tests 没有单独对 `older_authority_epoch` / `old_generation`
prepare reason code 各写一条 focused assertion；fan source 与 combined Runtime/command tests
仍有对应 fail-closed 行为证据。该窄 test-granularity gap 只记录为 software gap，不在 powered
hardware 上 replay/inject 历史 `FlightCommandEnvelope`，也不为 C3 设计 actuator replay fault。

【Final powered boundary；已完成历史记录】final retry 沿用已由 C1/C2 证明的 operating point
以减少变量：normal IMU auto activation；motor bus 与 LEFT ESC ON、RIGHT ESC physically OFF；OLD
Runtime ACTIVE 为 `left_pitch = captured ACTIVE-session baseline +0.05 rad`，其余三轴 captured
baseline hold，LEFT/RIGHT fan `0.05/0.0`；legal ACTIVE 后约 `3.0 s` 发送 SIGINT，OLD Runtime
最大 ACTIVE exposure `10 s`。这些值只记录已完成 session，不构成重新供电、prepare、signal
或重跑 C3 的授权。

【Pre-armed exact-PID trigger】final session 没有采用 ACTIVE 后 operator 手工寻找终端并按
Ctrl+C，而是使用同一个 local wrapper 分别配 `old_runtime.pid` 与 `new_runtime.pid`。
wrapper 通过 `ros2 pkg prefix windarmor_flight_control` 验证 installed executable/config，原子
写自身 PID 后 `exec` actual Runtime，PID 保持不变；它 foreground/no-daemon，不 prepare、
signal 或直接控制 actuator，并保存独立 `old_runtime.log`/`new_runtime.log` 与 exit status。

OLD helper 必须在 prepare 前验证 pidfile、PID、current UID、cmdline、`/proc` start time 和
running state，先观察 non-ACTIVE，再在初始 authority/ownership/PWM subscriptions 完成后实时
输出 `C3_STOP_READY` / `C3_STOP_ARMED`。经 `tee` 执行必须使用 `python3 -u`、显式 flush、
`pipefail` 与 `PIPESTATUS[0]`。只有同时观察到 ACTIVE/FLIGHT_CONTROL/actuation allowed、两
owner committed/token match、两 ownership 为同一 token 的 FLIGHT_CONTROL，且至少一帧同
token executable command 后，才开始约 3.0 s **连续** legal ACTIVE delay。期间任何新样本明确
显示 authority、owner、token 或 legal condition 丢失都锁存 FAIL，不恢复、不重启计时且不发送
SIGINT；到期再次核对 exact identity，只发送一次 SIGINT。helper 绝不发送 SIGTERM/SIGKILL/
SIGCONT，不 prepare、E-STOP、actuator control、kill、restart 或 retry。

final session 的 old-process graceful exit observation timeout 为 `5.0 s`。它是保守的
helper/operator timeout，不是 production actuator SLA：production cleanup 是非阻塞路径，现有
in-process
shutdown test 在两 revoke service 都 unavailable 时也同步返回。首次 powered session 从 SIGINT
到 helper 确认 OLD process disappearance 约 `1.027 s`，但 process exit 1，因此不能作为 clean
graceful timing PASS；final retry 的对应 observation 约为 `0.774782159 s` 且 exit 0。timeout
只导致 FAIL；while powered 时不得由 helper 自动 SIGKILL，operator 改走独立 E-STOP 与
physical kill。helper PASS 只表示 exact
old PID 收到 SIGINT、owner/PWM stop transition 与 old process disappearance 已观察，不能单独
宣布整个 C3 PASS。

【OLD required evidence】因果链为：legal ACTIVE / stable bounded actuation -> exact OLD PID
SIGINT -> old command stream permanently ceases -> explicit revoke 若实际送达则记录，否则
lower-level 0.25 s lease 后盾 -> motor halt、fan stop、motor/fan owner NONE -> OLD process
正常退出且无残留。要求 Terminal B 的 exit 0/no traceback、helper marker、continuous history
和 operator physical observation交叉闭合；不要求 shutdown 产生不存在的 safe-stop frame 或
final authority publication。

【Operator-controlled NEW half】helper 没有启动 NEW Runtime。OLD helper PASS、Terminal B
exit 0、owners NONE、PWM `[800,800]`、command cessation 和 operator physical stop 都确认后，
operator 才手工使用相同 wrapper + `new_runtime.pid` 启动 NEW Runtime。NEW PID 与 process
start identity 均不同，`authority_epoch` fresh；NEW 没有 prepare 或 ACTIVE。

restart 后使用约 `3.0 s` stable observation。这不是 production SLA；按当前 Runtime
`control_rate_hz=50 Hz` 可捕获约 150 条 fresh authority status，fan status/ownership 周期为
5 Hz，可捕获约 15 个周期样本，足以区分 OLD transient-local sample 与 NEW publisher。证据
审查以连续样本而非单条 durability cache 为准。required history 为 NEW stamp/identity 的
`DRY_RUN/NONE`、`authority_generation=0`、无 attempt、`actuation_allowed=false`、motor/fan
committed false、owners 持续 NONE/无 authority token、PWM `[800,800]`，restart 后没有
`FlightCommandEnvelope`、old target/token、automatic prepare/ACTIVE、legacy/manual takeover
或 actuator movement。

【Continuous evidence】默认 recorder 八项已包含 authority、Flight command、motor feedback/
safety/ownership 和 fan PWM/safety/ownership，authority/command 中已有 C3 identity fields，
因此不创建 C3 JSON config 或第二个 restart observer。recorder 必须从 OLD prepare 前持续到 NEW
stable DRY_RUN window 完成后；仅看到一条可能来自 durability cache 的 DRY_RUN 不足，必须用
new PID/start transcript、fresh `authority_epoch`、message stamp 和连续样本区分。

【Operator physical evidence】OLD ACTIVE 记录 LEFT fan 是否明确低幅旋转、selected motor 是否
符合批准的小幅行为，以及是否错轴/错侧或异常；shutdown 后记录 motor 与 LEFT fan 是否停止；
NEW startup/window 记录是否有任何 motor/fan movement、错轴/错侧及异常声音、振动、气味或
温升。精确 `+0.05 rad` target/feedback 和 token/timing 由 software evidence 负责，不要求肉眼
精确确认。

【PASS / safe exit closure】正式 C3 PASS 已结合上述 protocol pure-software evidence、OLD
graceful stop、fresh NEW DRY_RUN/NONE、no executable command/owner/target/movement 的 runtime+
hardware evidence。NEW stable window 后没有 prepare；NEW Runtime graceful exit 0，owners
保持 NONE、PWM stop，recorder 正常结束，LEFT ESC 与 motor bus 最终 physically OFF。C3 不
需要重跑，也不自动授权 C4。

【Normal launch cleanup observation】C3 完成后的 normal system shutdown 中，
`imu_driver_node`、`imu_motor_controller_node`、`fan_command_manager`、`fan_controller` 四个
launch child 均报告 `process has finished cleanly`；launch 随后输出
`Cannot shutdown a ROS adapter that is not running`。该项分类为
`LOW-PRIORITY / NON-C3-BLOCKING shutdown cleanup observation`：它发生在 C3 完成后，且
OLD/NEW Runtime 均 exit 0、actuator 已 fail-close、最终 power OFF，因此不降级 C3 PASS、
不触发 C3 重跑，也不在本任务扩大范围修复。

### C4a — Owner loss

【本阶段硬件状态】按单独批准的低幅 Gate B 场景供电。C4a 与 C4b 必须单独授权，不连续
执行。

【Trigger evidence】未来单独授权后，使用正常 lifecycle transition；保存 CLI 完整
stdout/stderr 和退出码：

```bash
ros2 lifecycle set /imu_motor_controller_node deactivate
```

【Continuous ROS evidence】默认 recorder topics 足够。required history 必须显示 motor
safety `node_active=false`、motor owner/state 消失或释放、motor halt，Runtime 检测 mismatch
后 inhibit，并使 fan stop/revoke；CLI 成功不能替代这些结果。

【Operator physical evidence】记录 motor 是否及时 halt、fan 是否及时 stop、是否有未批准
motor movement、错侧 fan 或继续转动，以及异常声音、振动、气味或温升。

【No automatic recovery evidence】若未来同一授权明确包含 lifecycle 恢复，恢复后不得自动
ACTIVE，不得恢复旧 epoch/generation/token/target、legacy owner 或 actuator 动作；transport
reconnect 同样不得恢复旧 Flight。

PASS：owner loss trigger、两域 fail-closed 和无自动恢复分别有证据。FAIL/立即停止：任一
actuator 继续或恢复旧 authority。安全退出：保持 fail-closed，先断 fan/ESC、后断 motor
bus，再退出进程；恢复命令留到新的独立授权。

### C4b — E-STOP interaction

【本阶段硬件状态】按单独批准的低幅 Gate B 场景供电。C4b 不与 C4a 连续执行。

【Trigger and timing evidence】未来单独授权后，必须在 prepare 前启动预热 watchdog，并把
该 session 的 stdout/stderr 和真实进程退出码保存为正式 timing evidence。可使用普通
`tee`；`<SESSION_DIR>` 必须先替换为 recorder 打印的实际目录：

```bash
cd ~/workspace/WindArmor
source /opt/ros/jazzy/setup.bash
source install/setup.bash
set -o pipefail
python3 scripts/flight_estop_watchdog.py 2>&1 \
  | tee <SESSION_DIR>/c4b_estop_watchdog.log
watchdog_status=${PIPESTATUS[0]}
printf 'WATCHDOG_EXIT_STATUS=%s\n' "$watchdog_status" \
  | tee -a <SESSION_DIR>/c4b_estop_watchdog.log
test "$watchdog_status" -eq 0
```

只有看到 `WATCHDOG READY` 后，操作员才可在另一终端按该子场景单独授权执行 prepare。
watchdog 只在首次同时观察到 ACTIVE、FLIGHT_CONTROL 和 `actuation_allowed=true` 后启动
monotonic 2.0 秒计时，并由同一预热 publisher 发布一次 `/e_stop=true`；10 秒内无 ACTIVE
也会发布并报告 `NO ACTIVE WITHIN TIMEOUT`。它不调用 prepare/reset、不控制 ownership，
也不发送 motor/fan command。required watchdog log 必须包含 `WATCHDOG READY`、
`ACTIVE DETECTED`、`E-STOP PUBLISHED`、`ACTIVE_TO_PUBLISH_SEC`、
`ESTOP OBSERVED BY FLIGHT`、`PUBLISH_TO_INHIBIT_SEC` 和 exit status。缺少任一项均为
`NOT VERIFIED`/FAIL；exit code 0 本身不是 timing PASS。`NO ACTIVE WITHIN TIMEOUT` 是
fail-closed abort evidence，不是 ACTIVE-to-E-STOP PASS。

【Continuous ROS evidence】默认 recorder topics 足够，必须在 watchdog/prepare 前就绪并
持续记录完整历史：ACTIVE、E-STOP 后 authority INHIBITED、两 owner NONE、motor/fan
latch、安全输出及无旧 command 恢复。watchdog monotonic log 负责严格 timing，recorder
负责 ROS topic history，二者互不替代。不得退回人工盯高速 topic、临时 awk/grep pipeline
或 ACTIVE 后新启动 `ros2 topic pub --once` 作 timing measurement。

【Operator physical evidence】记录 E-STOP 后 motor/fan 是否及时 halt/stop、是否有未批准
motor movement、错侧 fan 或继续转动，以及异常声音、振动、气味或温升。

【No automatic recovery evidence】`/e_stop=false`、transport reconnect、Runtime restart 或
未来另行授权的 latch reset 都不得自动 ACTIVE，不得恢复旧 epoch/generation/token/target、
legacy owner 或 actuator 动作。

预期 lower-level E-STOP 优先，Flight authority 失效。`/e_stop=false`、transport reconnect
或 Runtime restart 都不得自动恢复 Flight。真实 destructive fault、堵转、过温、过流、
带电拔线不属于正常 RC，禁止执行。

PASS：watchdog timing、continuous ROS、物理停止和无自动恢复四类证据全部闭合。
FAIL/立即停止：任一 actuator 继续、timing 超界/缺失或恢复旧 authority。安全退出：保持
E-STOP latch，先断 fan/ESC、后断 motor bus，再退出进程；恢复命令留到新的独立授权。

## 6. Gate D — Legacy + Final RC Regression

【本阶段需要通电】Raspberry Pi/IMU/CAN、CyberGear motor bus、双 fan/ESC。

【本阶段必须保持断电】无，但首次上电仍按 motor 与 fan 分步完成，不能一次授权推定
全部已安全。

【上电前检查】Gate A–C 全部 PASS；Flight Runtime 已退出、两 owner 为 NONE；robot
固定、fan guard/净空、E-STOP/physical kill就绪；既有零点、方向和低幅动作获准。

【Terminal 1】

```bash
cd ~/workspace/WindArmor
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch windarmor_bringup windarmor.launch.py
```

预期：系统按 v0.3.2 legacy 默认启动，motor MANUAL，fan 无 MANUAL/AUTO 授权且 stop；
不得存在 Flight Runtime 或 `/flight_control/command` publisher。

【Terminal 2】

```bash
ros2 topic echo /motors/control_mode std_msgs/msg/String
ros2 topic echo /fans/control_state std_msgs/msg/String
ros2 topic echo /fans/status_pwm std_msgs/msg/Int32MultiArray
ros2 topic echo /motor/status std_msgs/msg/String
```

【Terminal 3】按每项单独批准顺序执行：

```bash
ros2 service call /imu/set_zero std_srvs/srv/Trigger "{}"
ros2 service call /motors/set_zero std_srvs/srv/Trigger "{}"
ros2 service call /fans/manual_enable std_srvs/srv/SetBool "{data: true}"
ros2 topic pub --once /fans/pwm std_msgs/msg/Int32MultiArray "{data: [800, 800]}"
ros2 service call /fans/auto_enable std_srvs/srv/SetBool "{data: true}"
ros2 topic pub --once /e_stop std_msgs/msg/Bool "{data: true}"
```

MANUAL motor、HOME、LEGACY_AUTO 通过既有 motor keyboard 分别作用户批准的小幅测试；
fan MANUAL 必须先建立本次授权后的 `[800,800]` baseline；fan AUTO 只在 motor AUTO、
状态新鲜且本次显式授权后验证。不要同时运行互相竞争的发布者。

预期物理行为：MANUAL/HOME/LEGACY_AUTO 方向与既有契约一致；fan左右映射、MANUAL/
AUTO、ramp 和 stop 正常；E-STOP 同时停止全部 actuator。绝对不应该发生：Flight
owner/command、旧 Flight target、E-STOP clear 后自动动作、ERROR/reconnect 自动恢复。

确认 E-STOP 原因已排除且全部设备静止后，恢复本身需要单独授权，并逐条观察状态：

```bash
ros2 topic pub --once /e_stop std_msgs/msg/Bool "{data: false}"
ros2 service call /enable_motor std_srvs/srv/SetBool "{data: true}"
ros2 service call /fans/enable std_srvs/srv/SetBool "{data: true}"
ros2 service call /fans/reset_e_stop std_srvs/srv/Trigger "{}"
ros2 service call /fans/manual_enable std_srvs/srv/SetBool "{data: true}"
ros2 topic pub --once /fans/pwm std_msgs/msg/Int32MultiArray "{data: [800, 800]}"
```

恢复后 motor 只到 MANUAL，fan 只到本次 neutral baseline；不得自动进入 AUTO 或恢复
旧 PWM。restart 测试必须先按安全退出顺序停止并重新执行 Terminal 1 命令，启动后
再次确认 MANUAL、fan disarmed/stop 和无 Flight owner。

【PASS】依次覆盖 motor MANUAL、LEGACY_AUTO、HOME，fan MANUAL、LEGACY_AUTO，
E-STOP、shutdown、restart、explicit legacy reclaim 和 final normal regression；每项
都有实测证据且无回归。任一项 FAIL 或 NOT VERIFIED 都不得进入 RC/release。

【FAIL / 立即停止】任何模式/方向/通道错误、异常动作、安全机制退化、旧 Flight
authority 恢复或 shutdown 后输出残留。

【安全退出】

1. 发布 `/e_stop=true` 并确认 motor `EMERGENCY_STOP`、fan `[800,800]`；
2. 先断 fan/ESC 动力，再断 motor bus；
3. Terminal 1 按 `q` 或 `Ctrl+C`，确认全部 node 退出；
4. 最后断逻辑电源并保存日志、配置 snapshot 和 PASS/FAIL 记录。

Gate D 全部 PASS 后才允许进入 v0.4.0 RC/release 收口；这仍不自动授权 commit、push、
tag 或 GitHub Release。

## 7. Current software readiness and unresolved execution values

专用 verification controller、配置校验和 pure tests 已准备；它不新增 authority、
ownership、lease 或自动 test sequence。所有 Gate 的 repository interface 名称已按当前
source/config/launch/message/service 核对。

B1 已在 `left_pitch +0.05 rad`、其余 motor captured baseline hold、fan command
`0.0/0.0`、ESC 动力关闭且当时旧 GPIO12/13 信号断开的边界取得 functional hardware PASS，
不再仅为重复确认该结果而重跑。B1 closure run 的 `<3 sec` procedural timing 尚未闭合，
将在 Gate C / C4b E-STOP 子场景使用预热 watchdog 顺便取得；这不构成 C4b 执行授权。

新的 WindArmor GPIO12/26 runtime mapping 已在最终 B2 Flight session 中完成 LEFT bounded
验证：LEFT command `0.05` 达到 1210 us 后回到 800 us，RIGHT software command 保持
`0.0` 且 continuous PWM 始终为 800 us，四 motor baseline hold，无意外运动，E-STOP 后
authority/owner/latch/final PWM 均符合预期。RIGHT ESC 在该次 session 中独立断电，因此该
证据不扩展为 RIGHT powered physical Flight verification。B2 正式为 `HARDWARE PASS`，
Gate B 为 `COMPLETE`；C1 初次 attempt 与约 1 s retry 均为 `NOT VERIFIED`，3 s
helper-aborted session 不构成 attempt，最终 3 s retry 已取得 `HARDWARE PASS`。Gate C
的 C2 final Runtime-unresponsive session 也已取得 `HARDWARE PASS`；C3 首次 powered session
因 OLD Runtime SIGINT 后 exit 1 保持 `NOT VERIFIED` 历史，之后 production shutdown fix、
real-process signal smoke 和 final powered retry 三层证据闭合，C3 已取得 `HARDWARE PASS`。
C4 仍未执行、未授权，因此 Gate C 为
`IN PROGRESS / NOT COMPLETE`。

下列值必须在对应 Gate 执行前由用户明确决定：

- C4 的初始 bounded command 和最大持续时间仍为 `TO BE SET BEFORE EXECUTION`。

没有这些值不构成软件 blocker，但对应硬件命令不可执行。若真实验证暴露 bug，立即
停止硬件验证并创建只针对该缺陷的修复任务；不继续预先扩展 verification framework。
